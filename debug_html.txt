<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Where Winds Meet Codes (WWM) — Active Coupon Codes</title>
  <link rel="canonical" href="https://codes.yar.gg/">
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <meta name="description" content="Where Winds Meet (WWM) codes — copy, track, and easily redeem all the active coupon codes from a community-updated list.">
  <meta property="og:title" content="Where Winds Meet Codes (WWM)">
  <meta property="og:description" content="Where Winds Meet (WWM) codes — copy, track, and easily redeem all the active coupon codes from a community-updated list.">
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://codes.yar.gg/">
  <meta property="og:site_name" content="codes.yar.gg">
  <meta property="og:image" content="https://codes.yar.gg/apple-touch-icon.png">
  <meta name="twitter:card" content="summary">
  <meta name="twitter:title" content="Where Winds Meet Codes (WWM)">
  <meta name="twitter:description" content="Where Winds Meet (WWM) codes — copy, track, and easily redeem all the active coupon codes from a community-updated list.">
  <meta name="twitter:image" content="https://codes.yar.gg/apple-touch-icon.png">
  <meta name="theme-color" content="#482580">
  <script>document.documentElement.classList.add("js");</script>
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebApplication",
    "name": "Where Winds Meet Codes (WWM)",
    "url": "https://codes.yar.gg/",
    "description": "Where Winds Meet (WWM) codes — copy, track, and easily redeem all the active coupon codes from a community-updated list.",
    "applicationCategory": "GameApplication",
    "operatingSystem": "Any",
    "browserRequirements": "Requires JavaScript",
    "inLanguage": "en",
    "offers": {
      "@type": "Offer",
      "price": "0",
      "priceCurrency": "USD"
    }
  }
  </script>
  <style>
    :root {
      color-scheme: dark;
      --ink: #f4f7f8;
      --muted: #bdc8ce;
      --line: #2f3d45;
      --paper: #0d1216;
      --surface: #151c22;
      --surface-strong: #1b252c;
      --field: #10171c;
      --soft: #202b32;
      --copy: #35b8bd;
      --copy-dark: #24979c;
      --valid: #5dcc8c;
      --expired: #ff7474;
      --used: #b19cff;
      --shadow: 0 18px 46px rgba(0, 0, 0, 0.34);
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      background:
        linear-gradient(135deg, rgba(53, 184, 189, 0.16), transparent 34%),
        linear-gradient(315deg, rgba(255, 116, 116, 0.11), transparent 38%),
        radial-gradient(circle at top right, rgba(177, 156, 255, 0.10), transparent 34%),
        var(--paper);
    }

    button,
    input,
    select {
      font: inherit;
    }

    button {
      border: 0;
      cursor: pointer;
    }

    .page {
      width: min(1180px, calc(100% - 32px));
      margin: 0 auto;
      padding: 32px 0 56px;
    }

    .topbar {
      position: relative;
      z-index: 5;
      display: grid;
      grid-template-columns: minmax(0, 1fr) max-content;
      grid-template-areas:
        "title actions"
        "subtitle subtitle";
      align-items: flex-start;
      column-gap: 16px;
      row-gap: 6px;
      margin-bottom: 24px;
    }

    .brand {
      display: contents;
      min-width: 0;
    }

    .brand > div {
      display: contents;
    }

    h1 {
      grid-area: title;
      min-width: 0;
      max-width: 100%;
      margin: 0;
      font-size: clamp(1.65rem, 3.6vw, 2.8rem);
      line-height: 1.05;
      letter-spacing: 0;
    }

    .title-short {
      display: none;
    }

    .subtitle {
      grid-area: subtitle;
      margin: 0;
      color: var(--muted);
      max-width: none;
      font-size: 1rem;
    }

    .actions {
      grid-area: actions;
      display: flex;
      align-items: center;
      align-self: center;
      gap: 8px;
      flex-wrap: nowrap;
      justify-content: flex-end;
      justify-self: end;
      width: max-content;
      max-width: 100%;
      height: 40px;
      min-height: 40px;
      min-width: 32rem;
      overflow: hidden;
    }

    .actions.is-tutorial-hint-open {
      overflow: visible;
    }

    html.js:not(.shell-ready) .brand,
    html.js:not(.shell-ready) .actions,
    html.js:not(.shell-ready) .tabs {
      visibility: hidden;
    }

    html.js:not(.codes-ready) .carousel-copy {
      visibility: hidden;
    }

    .hotbar-primary {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      flex-wrap: nowrap;
    }

    .utility-button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      min-height: 40px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--surface);
      color: var(--ink);
      padding: 0 12px;
      box-shadow: 0 6px 18px rgba(24, 32, 38, 0.06);
      white-space: nowrap;
    }

    .utility-button:hover {
      border-color: #b9c6cd;
    }

    .support-link {
      border-color: #6f178c;
      background: transparent;
      color: #d98af2;
      text-decoration: none;
      padding-left: 9px;
      padding-right: 11px;
      line-height: 1;
      align-self: center;
    }

    .support-link svg {
      width: 18px;
      height: 18px;
      flex: 0 0 auto;
      display: block;
      stroke-width: 1.8;
    }

    .support-link span {
      display: inline-flex;
      align-items: center;
      line-height: 1;
      transform: translateY(0.5px);
    }

    .support-link:hover {
      border-color: #9b42bb;
      background: rgba(111, 23, 140, 0.16);
    }

    .mobile-copy-hint {
      display: none;
    }

    .submit-code-button {
      border-color: var(--valid);
      color: var(--valid);
    }

    .submit-code-button > span {
      display: inline-flex;
      align-items: center;
      gap: 0.25em;
      line-height: 1;
    }

    .submit-code-button:hover {
      border-color: var(--valid);
      background: color-mix(in srgb, var(--valid) 12%, transparent);
    }

    .icon-button {
      width: 40px;
      padding: 0;
    }

    .icon-button svg {
      display: block;
      margin: 0 auto;
      flex: 0 0 auto;
    }

    .desktop-tools {
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    .more-tools-picker {
      display: none;
      position: relative;
    }

    .help-button {
      position: relative;
      width: 28px;
      min-height: 40px;
      align-self: center;
      padding: 0;
      border-color: transparent;
      background: transparent;
      color: var(--muted);
      box-shadow: none;
      font-weight: 800;
      line-height: 1;
    }

    .tutorial-hint-active {
      outline: 0;
      border-color: #ffd166;
      background: rgba(255, 209, 102, 0.10);
      box-shadow: inset 0 0 0 2px #ffd166;
      border-radius: 999px;
    }

    .tutorial-hint-active::after {
      content: attr(data-tooltip);
      position: absolute;
      right: 0;
      top: calc(100% + 8px);
      z-index: 5;
      width: max-content;
      max-width: 220px;
      border: 1px solid #ffd166;
      border-radius: 8px;
      background: var(--surface-strong);
      color: var(--ink);
      padding: 7px 9px;
      box-shadow: var(--shadow);
      font-size: 0.82rem;
    }

    .help-button:hover {
      border-color: transparent;
      background: transparent;
      color: var(--ink);
    }

    .language-picker {
      position: relative;
    }

    #language-button span {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      line-height: 1;
      text-align: center;
    }

    .language-menu {
      position: absolute;
      left: 0;
      top: calc(100% + 8px);
      z-index: 4;
      min-width: 132px;
      max-height: min(360px, 70vh);
      overflow-y: auto;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--surface-strong);
      padding: 4px;
      box-shadow: var(--shadow);
    }

    .language-menu[hidden] {
      display: none;
    }

    .more-tools-menu {
      position: absolute;
      right: 0;
      top: calc(100% + 8px);
      z-index: 4;
      min-width: 168px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--surface-strong);
      padding: 4px;
      box-shadow: var(--shadow);
    }

    .more-tools-menu[hidden] {
      display: none;
    }

    .more-tools-option {
      display: flex;
      align-items: center;
      gap: 10px;
      width: 100%;
      border-radius: 6px;
      background: transparent;
      color: var(--ink);
      padding: 8px 10px;
      text-align: left;
      white-space: nowrap;
    }

    .more-tools-option svg {
      width: 16px;
      height: 16px;
      flex: 0 0 auto;
      stroke-width: 2;
    }

    .more-tools-option:hover,
    .more-tools-option:focus-visible {
      background: var(--soft);
    }

    .language-option {
      display: block;
      width: 100%;
      border-radius: 6px;
      background: transparent;
      color: var(--ink);
      padding: 8px 10px;
      text-align: left;
      white-space: nowrap;
    }

    .language-option:hover,
    .language-option:focus-visible,
    .language-option.is-selected {
      background: var(--soft);
    }

    .date-chip {
      position: relative;
      color: var(--muted);
    }

    .date-chip::after {
      content: attr(data-tooltip);
      position: absolute;
      right: 0;
      top: calc(100% + 8px);
      z-index: 3;
      width: max-content;
      max-width: 220px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--surface-strong);
      color: var(--ink);
      padding: 7px 9px;
      box-shadow: var(--shadow);
      font-size: 0.82rem;
      opacity: 0;
      pointer-events: none;
      transform: translateY(-3px);
      transition: opacity 140ms ease, transform 140ms ease;
    }

    .date-chip:hover::after,
    .date-chip.is-open::after,
    .date-chip:focus-visible::after {
      opacity: 1;
      transform: translateY(0);
    }

    .progress {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 14px;
      margin-bottom: 18px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: color-mix(in srgb, var(--surface) 86%, transparent);
      padding: 12px 14px;
    }

    .progress strong {
      font-size: 1.05rem;
      font-variant-numeric: tabular-nums;
      white-space: nowrap;
      min-width: 9ch;
    }

    .progress span {
      color: var(--muted);
    }

    #remaining-count {
      font-variant-numeric: tabular-nums;
      white-space: nowrap;
      min-width: 7ch;
    }

    .progress-track {
      flex: 1 1 auto;
      height: 10px;
      min-width: 120px;
      overflow: hidden;
      border-radius: 999px;
      background: var(--soft);
    }

    .progress-fill {
      width: 0%;
      height: 100%;
      border-radius: inherit;
      background: var(--copy);
      transition: width 180ms ease;
    }

    .carousel {
      display: grid;
      gap: 12px;
      margin-bottom: 18px;
      border: 1px solid rgba(18, 109, 114, 0.28);
      border-radius: 8px;
      background: color-mix(in srgb, var(--surface) 92%, transparent);
      padding: 16px;
      box-shadow: var(--shadow);
    }

    .carousel-info,
    .list-heading {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 10px;
      color: var(--muted);
      font-size: 0.92rem;
    }

    .archive-panel {
      margin-top: 30px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: color-mix(in srgb, var(--surface) 88%, transparent);
    }

    .archive-panel[hidden] {
      display: none;
    }

    .archive-summary {
      display: flex;
      flex-wrap: wrap;
      align-items: baseline;
      gap: 8px 14px;
      cursor: pointer;
      list-style: none;
      padding: 12px 14px;
      color: var(--muted);
      font-size: 0.92rem;
    }

    .archive-summary::-webkit-details-marker {
      display: none;
    }

    .archive-summary-main {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      color: var(--ink);
      font-size: 1rem;
    }

    .archive-summary-main strong {
      font-weight: 650;
    }

    .archive-count {
      display: inline-flex;
      align-items: center;
      min-height: 22px;
      border-radius: 999px;
      padding: 0 8px;
      background: var(--soft);
      color: var(--muted);
      font-size: 0.82rem;
      font-variant-numeric: tabular-nums;
    }

    .archive-summary-note {
      flex: 1 1 12rem;
    }

    .archive-summary::before {
      content: "";
      width: 0.45em;
      height: 0.45em;
      border-right: 2px solid currentColor;
      border-bottom: 2px solid currentColor;
      transform: rotate(-45deg);
      opacity: 0.7;
      transition: transform 120ms ease;
    }

    .archive-panel[open] > .archive-summary::before {
      transform: rotate(45deg);
    }

    .archive-panel > #archived-codes {
      padding: 0 14px 14px;
    }

    .new-codes:not(:empty) + .active-heading {
      margin-top: 20px;
    }

    .carousel-copy {
      display: grid;
      justify-items: stretch;
      align-items: center;
      align-content: center;
      gap: 8px;
      min-height: 168px;
      border: 1px solid rgba(53, 184, 189, 0.44);
      border-radius: 8px;
      background: linear-gradient(180deg, var(--surface-strong), color-mix(in srgb, var(--copy) 13%, var(--surface)));
      color: var(--ink);
      padding: 18px;
      text-align: center;
    }

    .carousel-copy.is-sliding-next {
      animation: carousel-slide-next 180ms ease-out;
    }

    .carousel-copy.is-sliding-previous {
      animation: carousel-slide-previous 180ms ease-out;
    }

    @keyframes carousel-slide-next {
      from {
        opacity: 0.42;
        transform: translateX(18px);
      }

      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @keyframes carousel-slide-previous {
      from {
        opacity: 0.42;
        transform: translateX(-18px);
      }

      to {
        opacity: 1;
        transform: translateX(0);
      }
    }

    @media (prefers-reduced-motion: reduce) {
      .carousel-copy.is-sliding-next,
      .carousel-copy.is-sliding-previous {
        animation: none;
      }
    }

    .carousel.is-used {
      border-color: color-mix(in srgb, var(--used) 62%, var(--line));
      box-shadow:
        var(--shadow),
        0 0 0 2px color-mix(in srgb, var(--used) 18%, transparent);
    }

    .carousel.is-used .carousel-copy {
      border-color: color-mix(in srgb, var(--used) 72%, var(--line));
      box-shadow: 0 0 0 3px color-mix(in srgb, var(--used) 14%, transparent);
    }

    .carousel.is-done {
      border-color: var(--copy);
      box-shadow:
        var(--shadow),
        0 0 0 2px color-mix(in srgb, var(--copy) 22%, transparent);
    }

    .carousel.is-done .carousel-copy {
      border-color: var(--copy);
      box-shadow: 0 0 0 4px color-mix(in srgb, var(--copy) 18%, transparent);
    }

    .carousel-copy:hover {
      border-color: var(--copy);
      box-shadow: 0 0 0 4px rgba(18, 109, 114, 0.10);
    }

    .carousel-copy strong {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
      font-size: clamp(2rem, 8vw, 4.8rem);
      letter-spacing: 0.04em;
      line-height: 1;
      min-height: 2em;
      text-align: center;
      direction: ltr;
      overflow-wrap: anywhere;
    }

    .carousel-copy span {
      width: 100%;
      color: var(--muted);
      font-size: 0.98rem;
      min-height: 2.6em;
      text-align: center;
    }

    .carousel-empty-note {
      color: var(--muted);
      font-size: 0.98rem;
    }

    .carousel-controls {
      display: grid;
      grid-template-columns: auto 1fr auto;
      gap: 8px;
    }

    .carousel-controls .utility-button {
      min-width: 42px;
    }

    .utility-button.danger {
      border-color: rgba(180, 61, 61, 0.36);
      color: var(--expired);
    }

    .utility-button:disabled {
      cursor: not-allowed;
      opacity: 0.48;
    }

    .filters {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 12px;
      margin: 18px 0;
      min-height: 42px;
    }

    .search {
      width: min(100%, 360px);
      height: 42px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--surface);
      color: var(--ink);
      padding: 0 14px;
      outline: none;
    }

    .search:focus {
      border-color: var(--copy);
      box-shadow: 0 0 0 3px rgba(18, 109, 114, 0.14);
    }

    .tabs {
      display: inline-grid;
      grid-auto-flow: column;
      grid-auto-columns: max-content;
      justify-content: start;
      min-width: 16.5rem;
      min-height: 42px;
      gap: 4px;
      padding: 4px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: color-mix(in srgb, var(--surface) 86%, transparent);
    }

    .tab {
      min-height: 34px;
      width: auto;
      border-radius: 6px;
      background: transparent;
      color: #d9e2e6;
      padding: 0 12px;
      white-space: nowrap;
    }

    .tab[aria-selected="true"] {
      background: var(--ink);
      color: var(--paper);
    }

    .codes {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(330px, 1fr));
      gap: 10px;
    }

    html.js:not(.codes-ready) #codes-cluster {
      height: 400px;
      min-height: 400px;
      overflow: hidden;
    }

    html.js:not(.codes-ready) #codes-cluster > * {
      visibility: hidden;
    }

    html.js:not(.codes-ready) #archive-panel {
      display: none !important;
    }

    .new-codes {
      grid-template-columns: repeat(auto-fill, minmax(330px, 1fr));
    }

    .code-card {
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      grid-template-areas: "code actions";
      gap: 6px;
      align-items: center;
      justify-content: start;
      min-height: 54px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: color-mix(in srgb, var(--surface) 90%, transparent);
      padding: 10px;
      box-shadow: 0 8px 22px rgba(24, 32, 38, 0.07);
    }

    .code-card.is-used {
      border-color: rgba(115, 93, 187, 0.45);
      background: color-mix(in srgb, var(--used) 8%, var(--surface));
    }

    .code-card.is-current {
      border-color: color-mix(in srgb, var(--valid) 76%, var(--line));
      box-shadow:
        inset 3px 0 0 var(--valid),
        0 8px 22px rgba(24, 32, 38, 0.07),
        0 0 0 2px color-mix(in srgb, var(--valid) 18%, transparent);
    }

    .code-card.is-new {
      border-color: color-mix(in srgb, var(--valid) 58%, var(--line));
      background: color-mix(in srgb, var(--valid) 8%, var(--surface));
    }

    .code-card.is-expired {
      border-color: rgba(180, 61, 61, 0.42);
      background: color-mix(in srgb, var(--expired) 8%, var(--surface));
    }

    .code-card.is-archived {
      grid-template-columns: minmax(0, 1fr) auto;
      grid-template-areas: "code meta";
      opacity: 0.76;
    }

    .code-card.is-archived .meta {
      display: flex;
    }

    .code-card.is-valid {
      border-color: rgba(35, 122, 72, 0.42);
      background: color-mix(in srgb, var(--valid) 8%, var(--surface));
    }

    .code-field {
      grid-area: code;
      width: 100%;
      height: 38px;
      min-width: 0;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--field);
      color: var(--ink);
      padding: 0 10px;
      font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
      direction: ltr;
      font-size: 0.94rem;
      letter-spacing: 0;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .list-copy svg,
    .list-action svg,
    .utility-button svg {
      width: 18px;
      height: 18px;
      stroke-width: 2;
    }

    #compare-codes svg {
      width: 22px;
      height: 22px;
    }

    .more-tools-option[data-more-action="compare"] svg {
      width: 22px;
      height: 22px;
    }

    .meta {
      display: none;
      align-items: center;
      gap: 8px;
      justify-content: flex-start;
      min-width: 0;
      color: var(--muted);
      font-size: 0.88rem;
    }

    .badge {
      display: inline-flex;
      align-items: center;
      min-height: 26px;
      max-width: 100%;
      border-radius: 999px;
      padding: 0 9px;
      background: var(--soft);
      color: var(--muted);
      white-space: nowrap;
    }

    .badge[hidden] {
      display: none;
    }

    .badge.used {
      background: rgba(115, 93, 187, 0.13);
      color: var(--used);
    }

    .badge.valid {
      background: rgba(35, 122, 72, 0.13);
      color: var(--valid);
    }

    .badge.expired {
      background: rgba(180, 61, 61, 0.12);
      color: var(--expired);
    }

    .list-actions {
      grid-area: actions;
      display: flex;
      gap: 6px;
      justify-content: flex-start;
      align-items: center;
      flex: 0 0 auto;
    }

    .list-copy,
    .list-action {
      display: inline-grid;
      place-items: center;
      width: 34px;
      height: 34px;
      border: 1px solid currentColor;
      border-radius: 8px;
      background: transparent;
      color: var(--muted);
    }

    .list-copy {
      color: var(--copy);
    }

    .list-action.used {
      color: var(--used);
    }

    .list-action.expired {
      color: var(--expired);
    }

    .list-copy:hover,
    .list-action:hover {
      background: color-mix(in srgb, currentColor 10%, transparent);
    }

    .list-action:disabled {
      cursor: not-allowed;
      opacity: 0.38;
    }

    .list-action[aria-pressed="true"] {
      background: color-mix(in srgb, currentColor 14%, transparent);
    }

    .list-copy:disabled {
      cursor: not-allowed;
      opacity: 0.38;
    }

    .code-date {
      position: relative;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 42px;
      height: 34px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: transparent;
      color: var(--muted);
      font-size: 0.72rem;
      line-height: 1;
      white-space: nowrap;
    }

    .code-date.expired-date {
      border-color: color-mix(in srgb, var(--expired) 75%, var(--line));
      color: var(--expired);
    }

    .code-date::after {
      content: attr(data-tooltip);
      position: absolute;
      right: 0;
      bottom: calc(100% + 8px);
      z-index: 4;
      width: max-content;
      max-width: min(270px, 72vw);
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--surface-strong);
      color: var(--ink);
      padding: 7px 9px;
      box-shadow: var(--shadow);
      font-size: 0.8rem;
      line-height: 1.25;
      text-align: left;
      white-space: normal;
      opacity: 0;
      pointer-events: none;
      transform: translateY(3px);
      transition: opacity 140ms ease, transform 140ms ease;
    }

    .code-date:hover::after,
    .code-date:focus-visible::after,
    .code-date.is-open::after {
      opacity: 1;
      transform: translateY(0);
    }

    .empty {
      display: none;
      margin-top: 22px;
      border: 1px dashed #b9c6cd;
      border-radius: 8px;
      background: color-mix(in srgb, var(--surface) 65%, transparent);
      padding: 28px;
      text-align: center;
      color: var(--muted);
    }

    .toast {
      position: fixed;
      right: 18px;
      bottom: 18px;
      display: flex;
      flex-direction: column-reverse;
      gap: 8px;
      width: min(360px, calc(100% - 36px));
      z-index: 20;
      pointer-events: none;
    }

    .toast-message {
      border-radius: 8px;
      background: var(--ink);
      color: var(--paper);
      padding: 12px 14px;
      box-shadow: var(--shadow);
      animation: toast-in 160ms ease-out;
    }

    .codes-update-banner {
      position: fixed;
      left: 50%;
      top: 16px;
      transform: translateX(-50%);
      z-index: 25;
      display: flex;
      align-items: center;
      gap: 12px;
      width: min(480px, calc(100% - 32px));
      border-radius: 8px;
      background: var(--ink);
      color: var(--paper);
      padding: 12px 14px;
      box-shadow: var(--shadow);
      animation: toast-in 160ms ease-out;
    }

    .codes-update-banner[hidden] {
      display: none !important;
    }

    .codes-update-banner p {
      margin: 0;
      flex: 1;
      font-size: 0.95rem;
      line-height: 1.35;
    }

    .codes-update-banner .dialog-button {
      flex: 0 0 auto;
    }

    @keyframes toast-in {
      from {
        opacity: 0;
      }
      to {
        opacity: 1;
      }
    }

    dialog {
      width: min(420px, calc(100% - 32px));
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--surface);
      color: var(--ink);
      padding: 0;
      box-shadow: var(--shadow);
    }

    #compare-dialog {
      width: min(1180px, calc(100% - 32px));
      max-height: calc(100vh - 40px);
    }

    #compare-dialog .dialog-body {
      display: flex;
      flex-direction: column;
      gap: 0;
      max-height: calc(100vh - 40px);
      overflow: auto;
    }

    dialog::backdrop {
      background: rgba(24, 32, 38, 0.45);
    }

    .dialog-body {
      padding: 20px;
    }

    .dialog-body h2 {
      margin: 0;
      font-size: 1.35rem;
    }

    .dialog-body p {
      margin: 10px 0 0;
      color: var(--muted);
      white-space: pre-line;
    }

    .dialog-code-row {
      display: grid;
      grid-template-columns: minmax(0, 1fr) 40px;
      gap: 8px;
      margin-top: 12px;
    }

    .dialog-code-row[hidden] {
      display: none;
    }

    .dialog-code {
      display: block;
      width: 100%;
      min-width: 0;
      border: 0;
      border-radius: 8px;
      background: var(--soft);
      color: var(--ink);
      padding: 10px 12px;
      font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
      text-align: left;
      overflow-wrap: anywhere;
    }

    .dialog-copy-button {
      display: inline-grid;
      place-items: center;
      width: 40px;
      min-height: 40px;
      border: 1px solid var(--copy);
      border-radius: 8px;
      background: transparent;
      color: var(--copy);
    }

    .dialog-copy-button svg {
      width: 18px;
      height: 18px;
      stroke-width: 2;
    }

    .dialog-code:hover,
    .dialog-copy-button:hover {
      background: color-mix(in srgb, var(--copy) 10%, var(--soft));
    }

    .confirm-check {
      display: flex;
      align-items: flex-start;
      gap: 8px;
      margin-top: 12px;
      color: var(--muted);
      font-size: 0.92rem;
      line-height: 1.35;
    }

    .confirm-check[hidden] {
      display: none;
    }

    .confirm-check input {
      width: 16px;
      height: 16px;
      margin-top: 2px;
      accent-color: var(--expired);
      flex: 0 0 auto;
    }

    .confirm-check.is-valid input {
      accent-color: var(--valid);
    }

    .expired-error-options {
      display: grid;
      gap: 10px;
      margin-top: 14px;
      border: 0;
      padding: 0;
    }

    .expired-error-options[hidden] {
      display: none;
    }

    .expired-error-options legend {
      margin: 0 0 10px;
      color: var(--ink);
      font-size: 0.92rem;
      font-weight: 650;
      line-height: 1.35;
    }

    .expired-error-option {
      display: flex;
      align-items: flex-start;
      gap: 8px;
      color: var(--muted);
      font-size: 0.9rem;
      line-height: 1.35;
    }

    .expired-error-option input {
      width: 16px;
      height: 16px;
      margin-top: 2px;
      accent-color: var(--expired);
      flex: 0 0 auto;
    }

    .dialog-input {
      width: 100%;
      height: 42px;
      margin-top: 12px;
      border: 1px solid var(--line);
      border-radius: 8px;
      background: var(--field);
      color: var(--ink);
      padding: 0 12px;
      font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
      text-transform: uppercase;
    }

    textarea.dialog-input {
      min-height: 128px;
      padding: 10px 12px;
      font-size: 16px;
      line-height: 1.45;
      resize: vertical;
      text-transform: none;
    }

    #compare-codes-input {
      min-height: 110px;
      font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
      font-size: 0.92rem;
    }

    .compare-results {
      display: grid;
      gap: 12px;
      margin-top: 14px;
    }

    .compare-results[hidden] {
      display: none;
    }

    .compare-group-title {
      display: flex;
      align-items: center;
      gap: 8px;
      margin: 0 0 6px;
      font-size: 0.86rem;
      font-weight: 650;
      color: var(--ink);
    }

    .compare-group-heading {
      display: inline-flex;
      align-items: center;
      flex-wrap: wrap;
      gap: 8px;
      min-width: 0;
    }

    @media (min-width: 761px) {
      #compare-dialog .dialog-body {
        display: grid;
        grid-template-columns: minmax(280px, 0.9fr) minmax(0, 1.4fr);
        grid-template-rows: auto auto minmax(220px, 1fr) auto;
        column-gap: 24px;
        row-gap: 10px;
        align-content: stretch;
        overflow: hidden;
      }

      #compare-dialog .dialog-body > h2 {
        grid-column: 1 / -1;
      }

      #compare-dialog .dialog-body > p[data-i18n="compareHelp"] {
        grid-column: 1 / -1;
        margin-top: 0;
      }

      #compare-codes-input {
        grid-column: 1;
        grid-row: 3;
        align-self: stretch;
        min-height: 0;
        height: 100%;
        resize: none;
        margin-top: 0;
      }

      #compare-empty,
      #compare-results {
        grid-column: 2;
        grid-row: 3;
        margin-top: 0;
        min-height: 0;
        max-height: none;
        overflow: auto;
      }

      #compare-dialog .compare-actions {
        grid-column: 1 / -1;
        margin-top: 8px;
      }
    }

    .compare-group-title .compare-count {
      color: var(--muted);
      font-weight: 500;
      font-variant-numeric: tabular-nums;
    }

    .compare-copy-button {
      width: 26px;
      height: 26px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      flex: 0 0 auto;
      border: 1px solid var(--line);
      border-radius: 6px;
      background: color-mix(in srgb, var(--surface) 88%, transparent);
      color: var(--ink);
      padding: 0;
    }

    .compare-copy-button svg {
      width: 14px;
      height: 14px;
      stroke-width: 2;
    }

    .compare-copy-button:hover:not(:disabled) {
      border-color: var(--copy);
      color: var(--copy);
    }

    .compare-copy-button:disabled {
      opacity: 0.45;
      cursor: default;
    }

    .compare-group.is-new .compare-group-title {
      color: var(--valid);
    }

    .compare-group.is-matched .compare-group-title {
      color: var(--used);
    }

    .compare-group.is-expired .compare-group-title {
      color: var(--expired);
    }

    .compare-group.is-unknown .compare-group-title {
      color: var(--muted);
      font-weight: 500;
    }

    .compare-codes {
      display: flex;
      flex-wrap: wrap;
      gap: 6px;
    }

    .compare-chip {
      border: 1px solid var(--line);
      border-radius: 6px;
      background: color-mix(in srgb, var(--surface) 88%, transparent);
      color: var(--ink);
      padding: 4px 8px;
      font-family: "Cascadia Mono", "SFMono-Regular", Consolas, monospace;
      font-size: 0.78rem;
      line-height: 1.2;
    }

    .compare-chip.is-new {
      border-color: color-mix(in srgb, var(--valid) 72%, var(--line));
      background: color-mix(in srgb, var(--valid) 12%, transparent);
      color: var(--valid);
    }

    .compare-chip.is-matched {
      border-color: color-mix(in srgb, var(--used) 72%, var(--line));
      background: color-mix(in srgb, var(--used) 12%, transparent);
      color: var(--used);
    }

    .compare-chip.is-expired {
      border-color: color-mix(in srgb, var(--expired) 72%, var(--line));
      background: color-mix(in srgb, var(--expired) 12%, transparent);
      color: var(--expired);
    }

    .compare-chip.is-unknown {
      color: var(--muted);
      opacity: 0.88;
    }

    .compare-empty {
      margin: 12px 0 0;
      color: var(--muted);
      font-size: 0.92rem;
    }

    .dialog-actions.compare-actions {
      justify-content: space-between;
      flex-wrap: wrap;
    }

    .dialog-actions.compare-actions .compare-actions-end {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      margin-left: auto;
    }

    .dialog-input:focus {
      border-color: var(--copy);
      box-shadow: 0 0 0 3px rgba(53, 184, 189, 0.14);
      outline: none;
    }

    .dialog-input.is-duplicate-active {
      border-color: var(--used);
      box-shadow: 0 0 0 3px rgba(177, 156, 255, 0.14);
    }

    .dialog-input.is-duplicate-expired {
      border-color: var(--expired);
      box-shadow: 0 0 0 3px rgba(255, 116, 116, 0.14);
    }

    .captcha-widget {
      display: flex;
      justify-content: flex-end;
      min-height: 65px;
      margin-top: 14px;
    }

    .captcha-widget[hidden] {
      display: none;
    }

    .dialog-actions {
      display: flex;
      align-items: center;
      justify-content: flex-end;
      gap: 8px;
      margin-top: 18px;
    }

    .new-code-message {
      flex: 1 1 auto;
      min-width: 0;
      margin: 0;
      color: var(--muted);
      font-size: 0.9rem;
      line-height: 1.25;
      text-align: left;
    }

    .new-code-message.is-active {
      color: var(--used);
    }

    .new-code-message.is-expired {
      color: var(--expired);
    }

    .dialog-button {
      flex: 0 0 auto;
      min-height: 38px;
      border-radius: 8px;
      padding: 0 13px;
      border: 1px solid var(--line);
      background: var(--surface);
      color: var(--ink);
    }

    .dialog-button:disabled {
      cursor: not-allowed;
      opacity: 0.48;
    }

    .dialog-button.primary {
      border-color: var(--copy);
      background: var(--copy);
      color: #ffffff;
    }

    .dialog-button.primary:disabled {
      border-color: rgba(255, 116, 116, 0.38);
      background: rgba(255, 116, 116, 0.08);
      color: color-mix(in srgb, var(--expired) 72%, var(--muted));
      cursor: not-allowed;
      opacity: 1;
    }

    body.tutorial-active::before {
      content: "";
      position: fixed;
      inset: 0;
      z-index: 24;
      background: rgba(13, 18, 22, 0.28);
      pointer-events: all;
    }

    .tutorial-popover {
      position: fixed;
      z-index: 30;
      width: max-content;
      min-width: min(280px, calc(100vw - 24px));
      max-width: min(520px, calc(100vw - 24px));
      max-height: calc(100vh - 24px);
      overflow-y: auto;
      overflow-x: hidden;
      border: 1px solid #ffd166;
      border-radius: 8px;
      background: var(--surface);
      color: var(--ink);
      padding: 12px;
      box-shadow:
        var(--shadow),
        0 0 0 3px rgba(255, 209, 102, 0.14);
    }

    .tutorial-popover[hidden] {
      display: none;
    }

    .tutorial-body {
      display: grid;
      gap: 9px;
    }

    .tutorial-body p {
      margin: 0;
      line-height: 1.35;
      font-size: 0.94rem;
      overflow-wrap: anywhere;
    }

    .tutorial-body h2 {
      margin: 0;
      font-size: 1.05rem;
    }

    .tutorial-body strong {
      color: #ffd166;
    }

    .tutorial-dots {
      display: flex;
      align-items: center;
      gap: 6px;
      flex: 1 0 64px;
      min-width: 64px;
    }

    .tutorial-dot {
      width: 8px;
      height: 8px;
      border-radius: 999px;
      background: var(--soft);
    }

    .tutorial-dot.is-active {
      background: var(--copy);
    }

    .tutorial-boundary {
      position: fixed;
      z-index: 25;
      border: 3px solid #ffd166;
      border-radius: 8px;
      box-shadow: 0 0 0 8px rgba(255, 209, 102, 0.18);
      pointer-events: none;
    }

    .tutorial-boundary[hidden] {
      display: none;
    }

    .tutorial-highlight {
      position: relative;
      z-index: 25;
      pointer-events: none;
    }

    .tutorial-highlight-hotbar {
      border-radius: 8px;
    }

    .tutorial-highlight-filters {
      border-radius: 8px;
    }

    .tutorial-highlight-danger {
      opacity: 1 !important;
      border-color: var(--expired) !important;
      background: color-mix(in srgb, var(--expired) 18%, var(--surface));
      color: #ffdede !important;
      box-shadow:
        0 0 0 3px #ffd166,
        0 0 0 8px rgba(255, 116, 116, 0.24),
        0 10px 24px rgba(255, 116, 116, 0.18);
    }

    .tutorial-actions {
      margin-top: 2px;
      gap: 6px;
      flex-wrap: wrap;
      justify-content: flex-end;
    }

    .tutorial-actions .dialog-button {
      min-width: 0;
      padding: 0 10px;
      white-space: normal;
    }

    .tutorial-actions .dialog-button.primary {
      color: #000000;
    }

    @media (min-width: 761px) {
      h1 {
        align-self: center;
        overflow: hidden;
        white-space: nowrap;
      }

      .subtitle {
        white-space: nowrap;
      }

      .actions {
        transform: translateY(3px);
      }

    }

    @media (min-width: 761px) and (max-width: 1032px) {
      .title-full {
        display: none;
      }

      .title-short {
        display: inline;
        font-size: clamp(1.45rem, 2.6vw, 2.2rem);
      }

      .submit-code-full {
        display: none;
      }
    }

    @media (max-width: 760px) {
      .page {
        width: min(100% - 18px, 1180px);
        padding-top: 22px;
      }

      .topbar,
      .filters {
        display: flex;
        align-items: stretch;
        flex-direction: column;
      }

      .topbar {
        position: relative;
        gap: 8px;
        margin-bottom: 12px;
      }

      .brand {
        display: flex;
        align-items: center;
        gap: 12px;
      }

      .brand > div {
        display: block;
      }

      h1 {
        padding-right: 40px;
      }

      .subtitle {
        margin: 6px 0 0;
        max-width: 720px;
        text-wrap: pretty;
      }

      .tabs {
        width: 100%;
        min-width: 0;
        grid-template-columns: repeat(4, minmax(0, 1fr));
        justify-content: stretch;
      }

      .tab,
      .search {
        width: 100%;
      }

      .actions {
        display: flex;
        justify-content: flex-end;
        align-items: center;
        flex-wrap: wrap;
        gap: 4px;
        width: 100%;
        max-width: none;
        height: auto;
        min-height: 34px;
        min-width: 0;
        overflow: visible;
      }

      .hotbar-primary {
        gap: 4px;
      }

      .actions .utility-button {
        width: auto;
        min-height: 34px;
        padding: 0 6px;
        font-size: 0.8rem;
        white-space: nowrap;
      }

      .actions .icon-button {
        width: 34px;
        padding: 0;
      }

      .actions .help-button {
        position: absolute;
        top: 0;
        right: 0;
        width: 22px;
        min-height: 22px;
        padding: 0;
        font-size: 0.78rem;
        z-index: 2;
      }

      .actions .language-menu {
        min-width: 118px;
      }

      .desktop-tools {
        display: none;
      }

      .more-tools-picker {
        display: inline-flex;
        align-items: center;
      }

      .actions .submit-code-button {
        gap: 3px;
        padding-left: 5px;
        padding-right: 6px;
      }

      .support-link {
        gap: 3px;
        padding-left: 5px;
        padding-right: 6px;
      }

      .support-link span {
        display: inline-flex;
        align-items: center;
        line-height: 1;
      }

      .support-link svg,
      .actions .utility-button svg {
        width: 16px;
        height: 16px;
      }

      .date-chip::after {
        left: 0;
        right: auto;
      }

      .submit-code-full {
        display: none;
      }

      .desktop-copy-hint {
        display: none;
      }

      .mobile-copy-hint {
        display: inline;
      }

      .carousel-copy {
        min-height: 112px;
        gap: 6px;
        padding: 12px;
      }

      .carousel-copy strong {
        font-size: clamp(1.85rem, 8vw, 3.2rem);
        min-height: 1.2em;
      }

      .carousel-copy span {
        font-size: 0.88rem;
        min-height: 0;
      }

      .progress {
        align-items: center;
        flex-direction: row;
        gap: 8px;
        margin-bottom: 10px;
        padding: 8px 10px;
      }

      .progress strong {
        flex: 0 0 9ch;
      }

      .progress strong,
      .progress span {
        font-size: 0.9rem;
        white-space: nowrap;
      }

      .progress-track {
        flex: 1 1 90px;
        width: auto;
        height: 8px;
        min-width: 0;
      }

      .carousel-controls {
        grid-template-columns: 42px minmax(0, 1fr) 42px;
      }

      .carousel-controls .utility-button {
        width: 100%;
        padding: 0 8px;
      }

      .codes {
        grid-template-columns: minmax(0, 1fr);
      }

      .code-card {
        grid-template-columns: minmax(0, 1fr) auto;
        grid-template-areas:
          "code actions";
        min-height: 52px;
        gap: 6px;
        padding: 8px;
      }

      .code-card.is-archived {
        grid-template-areas: "code meta";
      }

      .code-field {
        max-width: none;
        height: 34px;
        padding: 0 8px;
        font-size: 0.86rem;
      }

      .meta {
        display: none;
      }

      .list-actions {
        gap: 4px;
      }

      .list-copy,
      .list-action,
      .code-date {
        width: 32px;
        height: 32px;
      }

      .code-date {
        font-size: 0.68rem;
      }

    }

    @media (max-width: 760px) and (orientation: landscape) {
      .tutorial-popover {
        max-width: min(560px, calc(100vw - 24px));
      }

      .page {
        width: min(100% - 18px, 1180px);
        padding-top: 12px;
      }

      .topbar {
        margin-bottom: 10px;
      }

      .progress {
        margin-bottom: 10px;
      }

      .carousel {
        gap: 8px;
        margin-bottom: 10px;
        padding: 10px;
      }

      .carousel-info {
        font-size: 0.84rem;
      }

      .carousel-copy {
        min-height: 112px;
        gap: 6px;
        padding: 12px;
      }

      .carousel-copy strong {
        font-size: clamp(1.85rem, 8vw, 3.2rem);
      }

      .carousel-copy span {
        font-size: 0.88rem;
      }

      .codes {
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 8px;
      }

      .code-card {
        min-height: 46px;
        padding: 6px;
      }

      .code-field {
        height: 32px;
        font-size: 0.8rem;
      }

      .list-copy,
      .list-action,
      .code-date {
        width: 30px;
        height: 30px;
      }

      .code-date {
        font-size: 0.64rem;
      }
    }
  </style>
</head>
<body>
  <svg xmlns="http://www.w3.org/2000/svg" style="position:absolute;width:0;height:0;overflow:hidden" aria-hidden="true" focusable="false">
    <symbol id="icon-copy" viewBox="0 0 24 24" fill="none" stroke="currentColor">
      <rect width="14" height="14" x="8" y="8" rx="2"></rect>
      <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
    </symbol>
    <symbol id="icon-unused" viewBox="0 0 24 24" fill="none" stroke="currentColor">
      <rect x="5" y="5" width="14" height="14" rx="3"></rect>
    </symbol>
    <symbol id="icon-used" viewBox="0 0 24 24" fill="none" stroke="currentColor">
      <path d="m5 12 4 4L19 6"></path>
    </symbol>
    <symbol id="icon-expired" viewBox="0 0 304 288" fill="currentColor" stroke="none">
      <path d="M252.6 273.8c-5.4 2.6-10.6 3.6-16.3 3.6-26.6-.1-53.3-.5-80 .1-14.4.4-30.4-9-33.3-28.1-.5-3.5-2-4.6-5.1-5.5-46-13.3-75-43.4-83.6-90.4C22.8 90.8 64.1 34.7 122.8 22.9c62.2-12.5 120.2 25.6 133.9 87.6 5.9 26.5 2 51.9-10.4 76-1.9 3.7-1.9 6.5.2 10.1 6.3 10.4 12.2 21.1 18.2 31.6 9.6 16.9 4.8 35.1-12.1 45.6ZM95.9 50.4c-4.3 2.8-8.7 5.3-12.6 9.4 4.5 4.5 10 9.3 4.3 15.4-6.4 6.9-11.2.1-15.8-3.4-.6.3-1 .4-1.2.6-11.9 14.1-18.8 30.4-21.3 48.7-.5 4 1.6 4.6 5 4.7 5.7.2 8.9 3.3 9 8 .1 4.5-2.9 7.5-8.4 8.2-6 .8-6.5 1.5-5.4 7.5 1.7 8.7 4 17.2 7.9 25.2 3.7 7.8 8 15.2 14.2 21.7 4.8-4.1 9.3-10.2 15.5-4.3 6.8 6.5.2 11.2-4 16.2 12.5 10.3 26.1 17.3 41.5 20.6 2.9.6 3.4-1.5 4.3-3.1 11.1-19.4 22.2-38.8 33.4-58.1 1.6-2.9 1.2-4.8-1-7-6.6-6.4-13-13-19.5-19.4-2-2-4-4.2-4-7.2-.1-17.7-.2-35.3 0-53 .1-5.3 4.6-8.5 9.7-7.4 5.4 1.1 6.4 5.4 6.4 9.9.1 11 .7 22-.1 33-.8 10.1 1.4 18.2 10 24.1 2.5 1.7 4.5 4.1 6.5 6.3 2.2 2.6 4 2.4 6.7.4 6.6-4.9 14.2-7.2 22.5-6.1 11.6 1.4 20 7.5 25.6 17.7 2.7 4.9 5.3 9.9 9.2 14.9 4.2-8.5 6.2-16.8 7.8-25.2.9-4.8.2-6.6-5-6.7-6.5-.1-10.2-5.4-8.3-10.7 1.6-4.2 5.1-5.6 9.2-5.4 3.9.1 5.1-1.4 4.5-5.1-2.1-12.1-5.4-23.7-11.6-34.3-3.1-5.3-6.3-10.5-10.7-15.1-4.9 3.7-9.1 10.4-15.5 4.5-7.2-6.6 0-11.4 3.7-16.4-14-11.7-29.6-18.6-47-21.7-6.1-1.1-6.6-.8-7.5 5.4-.8 5.6-3.4 8.2-8.2 8.2-4.6-.1-7.4-3.2-7.9-8.6-.5-5.7-1-6.1-6.7-5.2-12.3 2-23.8 6-35.2 12.8Zm145.7 169.5c-10.2-17.8-20.3-35.6-30.6-53.3-7.2-12.4-22-13-29-.9-13.7 23.6-27.2 47.3-40.6 71.1-6.6 11.7.8 24.7 14.2 24.8 27 .2 54 .2 80.9 0 13.5-.1 20.9-12.7 14.7-24.6-2.9-5.6-6.2-11-9.6-17.1Z"></path>
      <path d="M187.9 204.8c.2-4.3-.3-8.1.6-11.9.9-4.1 3.4-6.5 7.6-6.5 4.5 0 7.5 2.5 7.9 6.8.5 5.6.5 11.3 0 16.9-.4 4.3-3.3 7-7.7 7-4.2.1-6.9-2.4-7.9-6.4-.4-1.7-.3-3.6-.5-5.9Z"></path>
      <path d="M204.1 240.7c-3.4 7.2-10.1 8.7-14 3.5-2-2.8-2.4-5.8-.6-8.9 1.9-3.2 5-4.3 8.5-3.5 4.3 1 6 4.3 6.1 8.9Z"></path>
    </symbol>
  </svg>
  <main class="page">
    <header class="topbar">
      <div class="brand">
        <div>
          <h1 id="page-title">
            <span class="title-full" data-i18n="pageTitle">Where Winds Meet Codes</span>
            <span class="title-short" data-i18n="pageTitleShort">WWM Codes</span>
          </h1>
          <p class="subtitle" data-i18n="subtitle">Copy, track, and easily redeem all the active WWM coupon codes from a community-updated list.</p>
        </div>
      </div>
      <div class="actions">
        <button class="utility-button help-button" id="tutorial-help" type="button" aria-label="Show tutorial" title="Show tutorial">?</button>
        <div class="hotbar-primary">
          <button class="utility-button icon-button date-chip" type="button" data-updated-at="2026-09-11T08:26:14.009Z" data-tooltip="Last updated __LAST_UPDATED_LOCAL__" aria-label="Last updated __LAST_UPDATED_LOCAL__" title="Last updated __LAST_UPDATED_LOCAL__">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <circle cx="12" cy="12" r="8"></circle>
              <path d="M12 8v5l3 2"></path>
            </svg>
          </button>
          <div class="language-picker" id="language-picker">
            <button class="utility-button icon-button" id="language-button" type="button" aria-haspopup="menu" aria-expanded="false" aria-label="Select language" title="Select language">
              <span aria-hidden="true">EN</span>
            </button>
            <div class="language-menu" id="language-menu" role="menu" hidden>
              <button class="language-option" type="button" role="menuitemradio" data-language="en">English</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="es">Español</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="fr">Français</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="de">Deutsch</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="it">Italiano</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="pt">Português</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="ru">Русский</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="tr">Türkçe</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="ar">العربية</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="zh">中文</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="ja">日本語</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="ko">한국어</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="th">ไทย</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="vi">Tiếng Việt</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="id">Bahasa Indonesia</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="ms">Bahasa Melayu</button>
              <button class="language-option" type="button" role="menuitemradio" data-language="pirate">Pirate</button>
            </div>
          </div>
          <button class="utility-button icon-button" id="contact-button" type="button" aria-label="Contact" title="Contact">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <rect width="20" height="16" x="2" y="4" rx="2"></rect>
              <path d="m22 7-10 6L2 7"></path>
            </svg>
          </button>
          <button class="utility-button submit-code-button" id="submit-code" type="button">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <path d="M12 5v14"></path>
              <path d="M5 12h14"></path>
            </svg>
            <span><span data-i18n="submitShort">Submit</span><span class="submit-code-full" data-i18n="submitFull"> code</span></span>
          </button>
        </div>
        <a class="utility-button support-link" href="https://ko-fi.com/Z3F0229G0L" target="_blank" rel="noopener" title="Ko-fi">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
            <path d="M3.7 7.15h12.4c.72 0 1.26.6 1.2 1.32l-.48 6.35c-.3 2.95-2.82 5.18-5.82 5.18h-1.2c-3 0-5.52-2.23-5.82-5.18l-.48-6.35c-.06-.72.48-1.32 1.2-1.32Z"></path>
            <path d="M17.3 9.35c2.55 0 4.15 1.22 4.15 3.05s-1.6 3.05-4.15 3.05"></path>
            <path fill="currentColor" stroke="none" d="M10.1 16.9c-2.6-2.05-4.3-3.65-4.3-5.65 0-1.55 1.25-2.6 2.75-2.45.7.07 1.2.45 1.55 1.15.35-.7.85-1.08 1.55-1.15 1.5-.15 2.75.9 2.75 2.45 0 2-1.7 3.6-4.3 5.65z"></path>
          </svg>
          <span data-i18n="tipJar">Ko-fi</span>
        </a>
        <div class="desktop-tools" id="desktop-tools">
          <button class="utility-button icon-button" id="compare-codes" type="button" aria-label="Compare code list" title="Compare code list">
            <svg viewBox="0 0 24 24" fill="currentColor" stroke="none" aria-hidden="true">
              <path d="m8 20-1.4-1.425L9.175 16H2v-2h7.175L6.6 11.425 8 10l5 5-5 5Zm8-6-5-5 5-5 1.4 1.425L14.825 8H22v2h-7.175l2.575 2.575L16 14Z"></path>
            </svg>
          </button>
          <button class="utility-button icon-button" id="copy-all" type="button" aria-label="Copy all active codes" title="Copy all active codes">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <rect width="14" height="14" x="8" y="8" rx="2"></rect>
              <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
            </svg>
          </button>
          <button class="utility-button icon-button" id="reset-used" type="button" aria-label="Reset code statuses" title="Reset code statuses">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <path d="M3 12a9 9 0 1 0 3-6.7"></path>
              <path d="M3 4v7h7"></path>
            </svg>
          </button>
        </div>
        <div class="more-tools-picker" id="more-tools-picker">
          <button class="utility-button icon-button" id="more-tools-button" type="button" aria-haspopup="menu" aria-expanded="false" aria-label="More tools" title="More tools">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
              <circle cx="12" cy="5" r="1.5"></circle>
              <circle cx="12" cy="12" r="1.5"></circle>
              <circle cx="12" cy="19" r="1.5"></circle>
            </svg>
          </button>
          <div class="more-tools-menu" id="more-tools-menu" role="menu" hidden>
            <button class="more-tools-option" type="button" role="menuitem" data-more-action="compare">
              <svg viewBox="0 0 24 24" fill="currentColor" stroke="none" aria-hidden="true">
                <path d="m8 20-1.4-1.425L9.175 16H2v-2h7.175L6.6 11.425 8 10l5 5-5 5Zm8-6-5-5 5-5 1.4 1.425L14.825 8H22v2h-7.175l2.575 2.575L16 14Z"></path>
              </svg>
              <span data-i18n="compareTitle">Compare code list</span>
            </button>
            <button class="more-tools-option" type="button" role="menuitem" data-more-action="export">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
                <rect width="14" height="14" x="8" y="8" rx="2"></rect>
                <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
              </svg>
              <span data-i18n="copyAllTitle">Copy all active codes</span>
            </button>
            <button class="more-tools-option" type="button" role="menuitem" data-more-action="reset">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
                <path d="M3 12a9 9 0 1 0 3-6.7"></path>
                <path d="M3 4v7h7"></path>
              </svg>
              <span data-i18n="resetTitle">Reset code statuses</span>
            </button>
          </div>
        </div>
      </div>
    </header>

    <section class="progress" aria-label="Code progress">
      <strong id="progress-label"><span id="used-count">0</span>/<span id="total-count">0</span> used</strong>
      <div class="progress-track" aria-hidden="true">
        <div class="progress-fill" id="progress-fill"></div>
      </div>
      <span id="remaining-count">0 left</span>
    </section>

    <section class="carousel" aria-label="Current coupon code">
      <div class="carousel-info">
        <span id="carousel-position">Code 1 of 1</span>
        <span class="badge" id="carousel-status" hidden></span>
      </div>
      <p class="carousel-empty-note" id="carousel-empty-note" hidden data-i18n="carouselEmpty">All active codes have been marked used.</p>
      <button class="carousel-copy" id="carousel-copy" type="button">
        <strong id="current-code">----------</strong>
        <span class="desktop-copy-hint" data-i18n="desktopCopyHint">Click to copy this code and move to the next one</span>
        <span class="mobile-copy-hint" data-i18n="mobileCopyHint">Tap to copy this code and move to the next one</span>
      </button>
      <div class="carousel-controls">
        <button class="utility-button" id="previous-code" type="button" aria-label="Previous code" title="Previous code">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
            <path d="m15 18-6-6 6-6"></path>
          </svg>
        </button>
        <button class="utility-button danger" id="report-previous" type="button" disabled data-i18n="previousExpired">Previous code was expired</button>
        <button class="utility-button" id="next-code" type="button" aria-label="Next code" title="Next code">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
            <path d="m9 18 6-6-6-6"></path>
          </svg>
        </button>
      </div>
    </section>

    <section class="filters" aria-label="Filter codes">
      <input class="search" id="search" type="search" placeholder="Search codes" autocomplete="off" data-i18n-placeholder="searchPlaceholder">
      <div class="tabs" role="tablist" aria-label="Code status">
        <button class="tab" type="button" role="tab" aria-selected="true" data-filter="all" data-i18n="filterAll">All</button>
        <button class="tab" type="button" role="tab" aria-selected="false" data-filter="unused" data-i18n="filterUnused">Unused</button>
        <button class="tab" type="button" role="tab" aria-selected="false" data-filter="used" data-i18n="filterUsed">Used</button>
        <button class="tab" type="button" role="tab" aria-selected="false" data-filter="expired" data-i18n="filterExpired">Expired</button>
      </div>
    </section>

    <div id="codes-cluster">
      <script>
        (function () {
          var el = document.currentScript.parentElement;
          var px = Math.max(400, Math.ceil(window.innerHeight - el.getBoundingClientRect().top));
          el.style.height = px + "px";
          el.style.minHeight = px + "px";
        })();
      </script>
      <div class="list-heading new-heading" id="new-heading" style="display: none">
        <strong data-i18n="newCodes">New codes</strong>
        <span data-i18n="newCodesNote">Added since your last visit</span>
      </div>
      <section class="codes new-codes" id="new-codes" aria-live="polite" style="display: none"></section>

      <div class="list-heading active-heading">
        <strong data-i18n="activeCodes">Active codes</strong>
        <span data-i18n="activeCodesNote">Used codes are marked off below</span>
      </div>
      <section class="codes" id="codes" aria-live="polite">
      </section>
      <p class="empty" id="empty" data-i18n="emptyFilter" style="display: none">No codes match the current filter.</p>
    </div>

    <details class="archive-panel" id="archive-panel" hidden>
      <summary class="archive-summary">
        <span class="archive-summary-main">
          <strong data-i18n="confirmedExpired">Confirmed expired</strong>
          <span class="archive-count" id="archive-count">0</span>
        </span>
        <span class="archive-summary-note" data-i18n="archiveNote">Archived codes kept for reference</span>
      </summary>
      <section class="codes" id="archived-codes" aria-label="Confirmed expired codes"></section>
    </details>
  </main>

  <dialog id="confirm-dialog">
    <form method="dialog" class="dialog-body">
      <h2 id="dialog-title" data-i18n="confirmReport">Confirm report</h2>
      <p id="dialog-message"></p>
      <div class="dialog-code-row" id="dialog-code-row">
        <button class="dialog-code" id="dialog-code" type="button" title="Copy code"></button>
        <button class="dialog-copy-button" id="dialog-copy-code" type="button" aria-label="Copy code" title="Copy code">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
            <rect width="14" height="14" x="8" y="8" rx="2"></rect>
            <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
          </svg>
        </button>
      </div>
      <fieldset class="expired-error-options" id="expired-error-row" hidden>
        <legend data-i18n="expiredErrorPrompt">Which error did the game show?</legend>
        <label class="expired-error-option">
          <input type="radio" name="expired-error" value="already-used">
          <span data-i18n="expiredErrorAlreadyUsed">Already used Redeem Code of the same type.</span>
        </label>
        <label class="expired-error-option">
          <input type="radio" name="expired-error" value="too-frequent">
          <span data-i18n="expiredErrorTooFrequent">Operated too frequently. Please try again later.</span>
        </label>
        <label class="expired-error-option">
          <input type="radio" name="expired-error" value="expired">
          <span data-i18n="expiredErrorExpired">Redeem code has expired.</span>
        </label>
        <label class="expired-error-option">
          <input type="radio" name="expired-error" value="attempts-used-up">
          <span data-i18n="expiredErrorAttemptsUsedUp">Attempts used up.</span>
        </label>
      </fieldset>
      <div class="captcha-widget" data-captcha-action="report-code" hidden></div>
      <div class="dialog-actions">
        <button class="dialog-button" id="dialog-cancel" value="cancel" type="submit" data-i18n="cancel">Cancel</button>
        <button class="dialog-button primary" id="dialog-confirm" value="confirm" type="submit" data-i18n="confirm">Confirm</button>
      </div>
    </form>
  </dialog>

  <dialog id="new-code-dialog">
    <form class="dialog-body" id="new-code-form">
      <h2 data-i18n="newCodeTitle">Submit new code</h2>
      <p data-i18n="newCodeHelp">Double-check that the code is currently valid before submitting it. If it is already expired, mark it for archive review instead.</p>
      <input class="dialog-input" id="new-code-input" name="code" type="text" maxlength="16" autocomplete="off" spellcheck="false" placeholder="CODE123" required>
      <label class="confirm-check">
        <input id="new-code-expired-check" type="checkbox">
        <span data-i18n="newCodeExpiredCheck">This code is expired and should be submitted for archive purposes.</span>
      </label>
      <label class="confirm-check is-valid">
        <input id="new-code-checked-check" type="checkbox" required>
        <span data-i18n="newCodeCheckedCheck">I checked this code before submitting it.</span>
      </label>
      <div class="captcha-widget" data-captcha-action="submit-code" hidden></div>
      <div class="dialog-actions">
        <p class="new-code-message" id="new-code-message" aria-live="polite"></p>
        <button class="dialog-button" id="new-code-cancel" type="button" data-i18n="cancel">Cancel</button>
        <button class="dialog-button primary" id="new-code-submit" type="submit" disabled data-i18n="submitCode">Submit code</button>
      </div>
    </form>
  </dialog>

  <dialog id="contact-dialog">
    <form class="dialog-body" id="contact-form">
      <h2 data-i18n="contactTitle">Contact</h2>
      <p data-i18n="contactHelp">Send a note about codes, bugs, or anything that would make the page more useful.</p>
      <textarea class="dialog-input" id="contact-message" name="message" maxlength="1200" autocomplete="off" spellcheck="true" required data-i18n-placeholder="contactPlaceholder"></textarea>
      <div class="captcha-widget" data-captcha-action="contact" hidden></div>
      <div class="dialog-actions">
        <button class="dialog-button" id="contact-cancel" type="button" data-i18n="cancel">Cancel</button>
        <button class="dialog-button primary" id="contact-submit" type="submit" disabled data-i18n="sendMessage">Send message</button>
      </div>
    </form>
  </dialog>

  <dialog id="compare-dialog">
    <form class="dialog-body" id="compare-form" method="dialog">
      <h2 data-i18n="compareTitle">Compare code list</h2>
      <p data-i18n="compareHelp">Paste the codes you already track. Comparison runs on this device against the site’s current active and expired lists.</p>
      <textarea class="dialog-input" id="compare-codes-input" name="codes" autocomplete="off" spellcheck="false" data-i18n-placeholder="comparePlaceholder" placeholder="CODE1&#10;CODE2, CODE3"></textarea>
      <p class="compare-empty" id="compare-empty" data-i18n="compareEmpty">Paste codes to see what’s new, matched, or expired.</p>
      <div class="compare-results" id="compare-results" hidden>
        <section class="compare-group is-new" id="compare-new-group" hidden>
          <h3 class="compare-group-title">
            <span class="compare-group-heading">
              <span data-i18n="compareNewTitle">New on site</span>
              <span class="compare-count" id="compare-new-count"></span>
              <button class="compare-copy-button" id="compare-copy-new" type="button" disabled aria-label="Copy new" title="Copy new">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
                <rect width="14" height="14" x="8" y="8" rx="2"></rect>
                <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
              </svg>
              </button>
            </span>
          </h3>
          <div class="compare-codes" id="compare-new-codes"></div>
        </section>
        <section class="compare-group is-matched" id="compare-matched-group" hidden>
          <h3 class="compare-group-title">
            <span class="compare-group-heading">
              <span data-i18n="compareMatchedTitle">Matched active</span>
              <span class="compare-count" id="compare-matched-count"></span>
              <button class="compare-copy-button" id="compare-copy-matched" type="button" disabled aria-label="Copy matched" title="Copy matched">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
                <rect width="14" height="14" x="8" y="8" rx="2"></rect>
                <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
              </svg>
              </button>
            </span>
          </h3>
          <div class="compare-codes" id="compare-matched-codes"></div>
        </section>
        <section class="compare-group is-expired" id="compare-expired-group" hidden>
          <h3 class="compare-group-title">
            <span class="compare-group-heading">
              <span data-i18n="compareExpiredTitle">Expired from your list</span>
              <span class="compare-count" id="compare-expired-count"></span>
              <button class="compare-copy-button" id="compare-copy-expired" type="button" disabled aria-label="Copy expired" title="Copy expired">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
                <rect width="14" height="14" x="8" y="8" rx="2"></rect>
                <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
              </svg>
              </button>
            </span>
          </h3>
          <div class="compare-codes" id="compare-expired-codes"></div>
        </section>
        <section class="compare-group is-unknown" id="compare-unknown-group" hidden>
          <h3 class="compare-group-title">
            <span class="compare-group-heading">
              <span data-i18n="compareUnknownTitle">Not on site</span>
              <span class="compare-count" id="compare-unknown-count"></span>
              <button class="compare-copy-button" id="compare-copy-missing" type="button" disabled aria-label="Copy missing" title="Copy missing">
              <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true">
                <rect width="14" height="14" x="8" y="8" rx="2"></rect>
                <path d="M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2"></path>
              </svg>
              </button>
            </span>
          </h3>
          <div class="compare-codes" id="compare-unknown-codes"></div>
        </section>
      </div>
      <div class="dialog-actions compare-actions">
        <button class="dialog-button primary" id="compare-mark-used" type="button" disabled data-i18n="compareMarkUsed">Mark used</button>
        <div class="compare-actions-end">
          <button class="dialog-button" id="compare-close" value="cancel" type="submit" data-i18n="cancel">Cancel</button>
        </div>
      </div>
    </form>
  </dialog>

  <div class="tutorial-popover" id="tutorial-dialog" role="dialog" aria-labelledby="tutorial-title" hidden>
    <div class="tutorial-body">
      <h2 id="tutorial-title">Copy the big code</h2>
      <p id="tutorial-message">Tap the big code to copy it, paste it into Where Winds Meet, then come back and repeat. The site will move through the list and keep track until all codes are used.</p>
      <div class="dialog-actions tutorial-actions">
        <div class="tutorial-dots" id="tutorial-dots" aria-hidden="true"></div>
        <button class="dialog-button" id="tutorial-skip" type="button">Skip</button>
        <button class="dialog-button" id="tutorial-previous" type="button">Back</button>
        <button class="dialog-button primary" id="tutorial-next" type="button">Next</button>
      </div>
    </div>
  </div>
  <div class="tutorial-boundary" id="tutorial-boundary" aria-hidden="true" hidden></div>

  <div class="toast" id="toast" aria-live="polite" aria-atomic="true"></div>

  <div class="codes-update-banner" id="codes-update-banner" hidden aria-live="polite">
    <p data-i18n="codesUpdatedBanner">Codes have been updated on the server.</p>
    <button class="dialog-button primary" type="button" id="codes-update-refresh" data-i18n="codesUpdatedRefresh">Refresh</button>
  </div>

  <script>
    document.documentElement.classList.add("shell-ready");

    let codeEntries = [];

    let archivedExpiredCodeEntries = [];

    let codes = [];
    let archivedExpiredCodes = [];
    let codeMetadataByCode = new Map();
    let appliedCodesFingerprint = "";
    let appliedCodesUpdatedAt = "";
    let pendingCodesUpdate = null;
    applyCodeData({ active: codeEntries, expired: archivedExpiredCodeEntries });
    appliedCodesFingerprint = codesFingerprint({ active: codeEntries, expired: archivedExpiredCodeEntries });

    const storageKey = "game-coupon-code-state-v1";
    const languageStorageKey = "game-coupon-code-language-v1";
    const tutorialStorageKey = "game-coupon-code-tutorial-seen-v1";
    const lastSeenStorageKey = "game-coupon-code-last-seen-build-v1";
    const cloudCodesCacheKey = "game-coupon-code-cloud-cache-v1";
    const submittedCodeStorageKey = "game-coupon-code-submitted-codes-v1";
    const browserSubmissionIdStorageKey = "game-coupon-code-browser-submission-id-v1";
    const browserSubmissionIdCreatedAtStorageKey = "game-coupon-code-browser-submission-id-created-at-v1";
    const establishedBrowserIdBackfillMs = 30 * 24 * 60 * 60 * 1000;
    const reportEndpoint = "/api/report-expired";
    const publicCodesUrl = "https://codes-backend.wwmcodes.workers.dev/v1/codes";
    const codesEndpoint = "/api/codes";
    const turnstileSiteKey = "0x4AAAAAADwFMXkCmYzFLSNx";
    const usedIconSvg = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true"><use href="#icon-used"></use></svg>`;
    const unusedIconSvg = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true"><use href="#icon-unused"></use></svg>`;
    const copyIconSvg = `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" aria-hidden="true"><use href="#icon-copy"></use></svg>`;
    const expiredIconSvg = `<svg viewBox="0 0 304 288" fill="currentColor" stroke="none" aria-hidden="true"><use href="#icon-expired"></use></svg>`;
    const translations = {
      en: {
        "pageTitle": "Where Winds Meet Codes",
        "pageTitleShort": "WWM Codes",
        "subtitle": "Copy, track, and easily redeem all the active WWM coupon codes from a community-updated list.",
        "lastUpdated": "Last updated {updatedAt}",
        "languageLabel": "Select language",
        "tutorialHelpTitle": "Show tutorial",
        "tutorialHintMessage": "New here? Tap for a quick tour.",
        "tutorialCopyTitle": "Copy the big code",
        "tutorialCopyTap": "Tap the code to copy it, paste it into Where Winds Meet, then repeat. The site will track used codes for you so they will never repeat when new codes are added.",
        "tutorialCopyClick": "Click the code to copy it, paste it into Where Winds Meet, then repeat. The site will track used codes for you so they will never repeat when new codes are added.",
        "tutorialReportTitle": "Report expired codes",
        "tutorialReportMessage": "If a code says it was <strong>Expired</strong>, not other error codes, report it so it can be reviewed and removed.",
        "tutorialHotbarTitle": "Use the hotbar",
        "tutorialHotbarMessage": "Check the last update time, change language, send feedback, and submit new codes in the hotbar.",
        "tutorialSearchTitle": "Search and filter",
        "tutorialSearchMessage": "Search and filter codes if desired.",
        "tutorialSkip": "Skip",
        "tutorialBack": "Back",
        "tutorialNext": "Next",
        "tutorialDone": "Done",
        "submitShort": "Submit",
        "submitFull": " code",
        "contact": "Contact",
        "tipJar": "Ko-fi",
        "compareTitle": "Compare code list",
        "compareHelp": "Paste the codes you already track. Comparison runs on this device against the site’s current active and expired lists.",
        "comparePlaceholder": "CODE1\nCODE2, CODE3",
        "compareEmpty": "Paste codes to see what’s new, matched, or expired.",
        "compareNewTitle": "New on site",
        "compareMatchedTitle": "Matched active",
        "compareExpiredTitle": "Expired from your list",
        "compareUnknownTitle": "Not on site",
        "compareMarkUsed": "Mark used",
        "compareMarkUsedTitle": "Mark pasted codes that exist on this site as used",
        "compareMarkedUsed": "Marked {count} pasted codes as used.",
        "compareNoneToMark": "No pasted codes matched the site list.",
        "compareCopyNew": "Copy new",
        "compareCopyMatched": "Copy matched",
        "compareCopyExpired": "Copy expired",
        "compareCopyMissing": "Copy missing",
        "compareCopiedMatched": "Copied {count} matched codes.",
        "compareCopiedExpired": "Copied {count} expired codes.",
        "moreToolsTitle": "More tools",
        "compareCopiedNew": "Copied {count} new codes.",
        "compareCopiedMissing": "Copied {count} missing codes.",
        "compareNothingToCopy": "Nothing to copy.",
        "copyAllTitle": "Copy all active codes",
        "resetTitle": "Reset code statuses",
        "progressUsed": "{used}/{total} used",
        "remaining": "{count} left",
        "carouselPosition": "Code {current} of {total}",
        "carouselNoRemaining": "No codes remaining",
        "carouselEmpty": "All active codes have been marked used.",
        "carouselDone": "All Done ✓",
        "carouselNoUnusedHint": "No unused codes remaining, unmark some or wait for new codes to be added.",
        "desktopCopyHint": "Click to copy this code and move to the next one",
        "mobileCopyHint": "Tap to copy this code and move to the next one",
        "previousTitle": "Previous code",
        "nextTitle": "Next code",
        "previousExpired": "Previous code was expired",
        "copyFirst": "Copy a code first",
        "reportAsExpired": "Report {code} as expired",
        "searchPlaceholder": "Search codes",
        "filterAll": "All",
        "filterUnused": "Unused",
        "filterUsed": "Used",
        "filterExpired": "Expired",
        "newCodes": "New codes",
        "newCodesNote": "Added since your last visit",
        "activeCodes": "Active codes",
        "activeCodesNote": "Used codes are marked off below",
        "emptyFilter": "No codes match the current filter.",
        "confirmedExpired": "Confirmed expired",
        "archiveNote": "Archived codes kept for reference",
        "statusUnused": "Not copied",
        "statusUsed": "Used",
        "statusExpired": "Expired",
        "couponCodeLabel": "Coupon code {code}",
        "archivedCodeLabel": "Archived expired coupon code {code}",
        "copyCode": "Copy code",
        "copyCodeLabel": "Copy {code}",
        "markUnused": "Mark unused",
        "markUsed": "Mark used",
        "markUnusedLabel": "Mark {code} as unused",
        "markUsedLabel": "Mark {code} as used",
        "unmarkExpired": "Unmark expired",
        "markExpired": "Mark expired",
        "unmarkExpiredLabel": "Unmark {code} as expired",
        "markExpiredLabel": "Mark {code} as expired",
        "addedTooltip": "Added to site on {addedAt} in local time.",
        "expiredTooltip": "Added to site on {addedAt} and marked expired on {expiredAt} in local time.",
        "verifiedTooltipPart": " Last verified on {verifiedAt}.",
        "lastVerifiedLine": "Last verified on {verifiedAt} in local time.",
        "confirmReport": "Confirm report",
        "reportExpiredTitle": "Report expired code?",
        "unmarkExpiredTitle": "Unmark expired code?",
        "reportExpiredMessage": "Please check the code again, then select the exact error message shown in the game. If you play in a different language, change it at the top of the site first.",
        "unmarkExpiredMessage": "Confirm this if the expired mark was a mistake. A correction will be submitted for review.",
        "expiredConfirmCheck": "I checked that this code is actually expired, not already redeemed or rate-limited.",
        "expiredErrorPrompt": "Which error did the game show?",
        "expiredErrorAlreadyUsed": "Already used Redeem Code of the same type.",
        "expiredErrorTooFrequent": "Operated too frequently. Please try again later.",
        "expiredErrorExpired": "Redeem code has expired.",
        "expiredErrorAttemptsUsedUp": "Attempts used up.",
        "expiredAlreadyUsedTitle": "Code is still active",
        "expiredAlreadyUsedHelp": "This error means you've already used this code before, but it's still active. If you have a list you referenced before, you can import it with the compare feature at the top of the site.",
        "expiredTooFrequentTitle": "Try again later",
        "expiredTooFrequentHelp": "This error means you entered the codes too quickly. Wait a second, then try again.",
        "cancel": "Cancel",
        "confirm": "Confirm",
        "reportExpired": "Report expired",
        "newCodeTitle": "Submit new code",
        "newCodeHelp": "Double-check that the code is currently valid before submitting it. If it is already expired, mark it for archive review instead.",
        "newCodeExpiredCheck": "This code is expired and should be submitted for archive purposes.",
        "newCodeCheckedCheck": "I checked this code before submitting it.",
        "submitCode": "Submit code",
        "contactTitle": "Contact",
        "contactHelp": "Send a note about codes, bugs, or anything that would make the page more useful. If you want a reply, include your UID so I can contact you.",
        "contactPlaceholder": "Type your message here...",
        "sendMessage": "Send message",
        "messageRequired": "Type a message before sending.",
        "messageSent": "Message sent. Thank you.",
        "messageFailed": "Message could not be sent from this preview.",
        "copiedAdvance": "{code} copied. Moving to the next code.",
        "copiedUsed": "{code} copied and marked used.",
        "copyFailed": "Copy failed. Select the text box and copy it manually.",
        "captchaLoading": "Verification is still loading. Please try again in a moment.",
        "captchaRequired": "Complete the verification before submitting.",
        "captchaFailed": "Verification failed. Please try again.",
        "captchaFailedDetail": "Verification failed ({reason}). Please try again.",
        "addedBack": "{code} added back to the carousel.",
        "markedUsed": "{code} marked used.",
        "noActiveCodes": "No active codes to copy.",
        "copyAllConfirm": "Copy all {count} active, unexpired codes to the clipboard?",
        "activeCopied": "Active codes copied for export.",
        "copyAllFailed": "Copy all failed. Try copying codes one at a time.",
        "copied": "{code} copied.",
        "reportedExpired": "{code} reported expired.",
        "restored": "{code} restored and correction sent.",
        "submittedExpiredArchive": "{code} submitted for expired archive review.",
        "submittedReview": "{code} submitted for review.",
        "expiredPreview": "{code} marked expired locally, but the report could not be sent.",
        "restoredPreview": "{code} restored locally, but the correction could not be sent.",
        "submitPreview": "{code} could not be submitted. Try again.",
        "reportRateLimited": "Too many reports right now. Wait a bit and try again.",
        "alreadyExpiredReport": "{code} is already marked expired.",
        "alreadyExpiredReportStale": "{code} is already marked expired. Your page may be out of date — tap Refresh for the latest codes.",
        "codeNotListedReport": "{code} is not on the active list, so it could not be reported.",
        "codeNotListedReportStale": "{code} is not on the active list. Your page may be out of date — tap Refresh for the latest codes.",
        "codesUpdatedBanner": "Codes have been updated on the server.",
        "codesUpdatedRefresh": "Refresh",
        "duplicateExpired": "Code is already in the list and expired.",
        "duplicateActive": "Code is already in the active list.",
        "duplicateExpiredStale": "That code is already listed as expired. Your page may be out of date — tap Refresh for the latest codes.",
        "duplicateActiveStale": "That code is already on the active list. Your page may be out of date — tap Refresh for the latest codes.",
        "duplicateSubmitted": "This code has already been submitted for review, thank you!",
        "tooLong": "Codes must be 16 characters or fewer.",
        "confirmChecked": "Confirm that you checked the code first.",
        "resetConfirm": "Reset the whole local code list? This clears copied, used, and expired statuses on this device.",
        "resetDone": "Local code statuses reset."
      }
    };
    const translationLoads = Object.create(null);

    const supportedLanguages = ["en", "es", "fr", "de", "it", "pt", "ru", "tr", "ar", "zh", "ja", "ko", "th", "vi", "id", "ms", "pirate"];
    const languageHtmlLang = {
      en: "en",
      es: "es",
      fr: "fr",
      de: "de",
      it: "it",
      pt: "pt",
      ru: "ru",
      tr: "tr",
      ar: "ar",
      zh: "zh-Hans",
      ja: "ja",
      ko: "ko",
      th: "th",
      vi: "vi",
      id: "id",
      ms: "ms",
      pirate: "en"
    };
    const languageDirections = {
      ar: "rtl"
    };
    const languageButtonLabels = {
      en: "EN",
      es: "ES",
      fr: "FR",
      de: "DE",
      it: "IT",
      pt: "PT",
      ru: "RU",
      tr: "TR",
      ar: "AR",
      zh: "文",
      ja: "日",
      ko: "한",
      th: "TH",
      vi: "VI",
      id: "ID",
      ms: "MS",
      pirate: "PI"
    };
    const tutorialSteps = [
      {
        titleKey: "tutorialCopyTitle",
        target: "#carousel-copy",
        placement: "bottom",
        message: () => t(window.matchMedia("(pointer: coarse)").matches ? "tutorialCopyTap" : "tutorialCopyClick")
      },
      {
        titleKey: "tutorialReportTitle",
        target: "#report-previous",
        placement: "top",
        messageHtmlKey: "tutorialReportMessage",
        highlightClass: "tutorial-highlight-danger"
      },
      {
        titleKey: "tutorialHotbarTitle",
        target: ".actions",
        highlightTargets: [".hotbar-primary", ".support-link", ".desktop-tools", ".more-tools-picker"],
        placement: "bottom",
        messageKey: "tutorialHotbarMessage",
        highlightClass: "tutorial-highlight-hotbar"
      },
      {
        titleKey: "tutorialSearchTitle",
        target: ".filters",
        placement: "top",
        messageKey: "tutorialSearchMessage",
        highlightClass: "tutorial-highlight-filters"
      }
    ];
    const state = loadState();
    let currentLanguage = loadLanguage();
    const previousSeenBuildAt = loadLastSeenBuild();
    let activeFilter = "all";
    let archivePinnedOpen = false;
    let archiveSearchExpanded = false;
    let archiveOpenSync = false;
    let pendingReport = null;
    let visitCarouselCodes = getUnusedCodes();
    let currentIndex = visitCarouselCodes.length ? codes.indexOf(visitCarouselCodes[0]) : -1;
    let lastCopiedCode = null;
    let pendingCarouselAnimationDirection = 0;
    let tutorialStepIndex = 0;
    let tutorialTargetEls = [];
    let tutorialHintVisible = false;
    let browserSubmissionIdFallback = "";
    let browserSubmissionIdCreatedAtFallback = "";
    let captchaRenderAttempts = 0;
    const captchaEnabled = Boolean(turnstileSiteKey);
    const captchaWidgetIds = new Map();

    const codesEl = document.querySelector("#codes");
    const codesClusterEl = document.querySelector("#codes-cluster");
    const newCodesEl = document.querySelector("#new-codes");
    const newHeadingEl = document.querySelector("#new-heading");
    const archivedCodesEl = document.querySelector("#archived-codes");
    const archivePanelEl = document.querySelector("#archive-panel");
    const archiveCountEl = document.querySelector("#archive-count");
    const emptyEl = document.querySelector("#empty");
    const searchEl = document.querySelector("#search");
    const progressLabelEl = document.querySelector("#progress-label");
    const remainingCountEl = document.querySelector("#remaining-count");
    const currentCodeEl = document.querySelector("#current-code");
    const carouselCopyEl = document.querySelector("#carousel-copy");
    const desktopCopyHintEl = document.querySelector(".desktop-copy-hint");
    const mobileCopyHintEl = document.querySelector(".mobile-copy-hint");
    const carouselPositionEl = document.querySelector("#carousel-position");
    const carouselEl = document.querySelector(".carousel");
    const carouselEmptyNoteEl = document.querySelector("#carousel-empty-note");
    const previousCodeEl = document.querySelector("#previous-code");
    const nextCodeEl = document.querySelector("#next-code");
    const reportPreviousEl = document.querySelector("#report-previous");
    const dialog = document.querySelector("#confirm-dialog");
    const dialogTitle = document.querySelector("#dialog-title");
    const dialogMessage = document.querySelector("#dialog-message");
    const dialogCodeRow = document.querySelector("#dialog-code-row");
    const dialogCode = document.querySelector("#dialog-code");
    const dialogCopyCode = document.querySelector("#dialog-copy-code");
    const dialogCancel = document.querySelector("#dialog-cancel");
    const dialogConfirm = document.querySelector("#dialog-confirm");
    const expiredErrorRow = document.querySelector("#expired-error-row");
    const expiredErrorInputs = [...document.querySelectorAll("input[name='expired-error']")];
    const toastEl = document.querySelector("#toast");
    const codesUpdateBannerEl = document.querySelector("#codes-update-banner");
    const codesUpdateRefreshEl = document.querySelector("#codes-update-refresh");
    const carouselStatusEl = document.querySelector("#carousel-status");
    const newCodeDialog = document.querySelector("#new-code-dialog");
    const newCodeForm = document.querySelector("#new-code-form");
    const newCodeInput = document.querySelector("#new-code-input");
    const newCodeMessage = document.querySelector("#new-code-message");
    const newCodeExpiredCheck = document.querySelector("#new-code-expired-check");
    const newCodeCheckedCheck = document.querySelector("#new-code-checked-check");
    const newCodeSubmit = document.querySelector("#new-code-submit");
    const contactDialog = document.querySelector("#contact-dialog");
    const contactForm = document.querySelector("#contact-form");
    const contactMessage = document.querySelector("#contact-message");
    const contactSubmit = document.querySelector("#contact-submit");
    const compareDialog = document.querySelector("#compare-dialog");
    const compareCodesButton = document.querySelector("#compare-codes");
    const compareCodesInput = document.querySelector("#compare-codes-input");
    const compareEmptyEl = document.querySelector("#compare-empty");
    const compareResultsEl = document.querySelector("#compare-results");
    const compareMarkUsedEl = document.querySelector("#compare-mark-used");
    const compareCopyButtons = {
      new: document.querySelector("#compare-copy-new"),
      matched: document.querySelector("#compare-copy-matched"),
      expired: document.querySelector("#compare-copy-expired"),
      unknown: document.querySelector("#compare-copy-missing")
    };
    let lastCompareBuckets = { new: [], matched: [], expired: [], unknown: [] };
    const compareGroups = {
      new: {
        section: document.querySelector("#compare-new-group"),
        codes: document.querySelector("#compare-new-codes"),
        count: document.querySelector("#compare-new-count"),
        copy: compareCopyButtons.new
      },
      matched: {
        section: document.querySelector("#compare-matched-group"),
        codes: document.querySelector("#compare-matched-codes"),
        count: document.querySelector("#compare-matched-count"),
        copy: compareCopyButtons.matched
      },
      expired: {
        section: document.querySelector("#compare-expired-group"),
        codes: document.querySelector("#compare-expired-codes"),
        count: document.querySelector("#compare-expired-count"),
        copy: compareCopyButtons.expired
      },
      unknown: {
        section: document.querySelector("#compare-unknown-group"),
        codes: document.querySelector("#compare-unknown-codes"),
        count: document.querySelector("#compare-unknown-count"),
        copy: compareCopyButtons.unknown
      }
    };
    const moreToolsPickerEl = document.querySelector("#more-tools-picker");
    const moreToolsButtonEl = document.querySelector("#more-tools-button");
    const moreToolsMenuEl = document.querySelector("#more-tools-menu");
    const dateChipEl = document.querySelector(".date-chip");
    const languagePickerEl = document.querySelector("#language-picker");
    const languageButtonEl = document.querySelector("#language-button");
    const languageMenuEl = document.querySelector("#language-menu");
    const contactButtonEl = document.querySelector("#contact-button");
    const copyAllEl = document.querySelector("#copy-all");
    const resetUsedEl = document.querySelector("#reset-used");
    const tutorialHelpEl = document.querySelector("#tutorial-help");
    const tutorialDialog = document.querySelector("#tutorial-dialog");
    const tutorialBoundaryEl = document.querySelector("#tutorial-boundary");
    const tutorialTitleEl = document.querySelector("#tutorial-title");
    const tutorialMessageEl = document.querySelector("#tutorial-message");
    const tutorialDotsEl = document.querySelector("#tutorial-dots");
    const tutorialSkipEl = document.querySelector("#tutorial-skip");
    const tutorialPreviousEl = document.querySelector("#tutorial-previous");
    const tutorialNextEl = document.querySelector("#tutorial-next");
    const captchaWidgets = [...document.querySelectorAll(".captcha-widget")];

    copyAllEl.addEventListener("click", copyAllCodes);
    resetUsedEl.addEventListener("click", resetState);
    tutorialHelpEl.addEventListener("click", () => {
      dismissTutorialHint();
      openTutorial();
    });
    document.querySelector("#submit-code").addEventListener("click", openNewCodeDialog);
    contactButtonEl.addEventListener("click", openContactDialog);
    compareCodesButton.addEventListener("click", openCompareDialog);
    compareCodesInput.addEventListener("input", renderCompareResults);
    compareMarkUsedEl.addEventListener("click", markComparedCodesUsed);
    Object.entries(compareCopyButtons).forEach(([kind, button]) => {
      button.addEventListener("click", () => copyCompareBucket(kind));
    });
    moreToolsButtonEl.addEventListener("click", toggleMoreToolsMenu);
    moreToolsMenuEl.addEventListener("click", chooseMoreTool);
    languageButtonEl.addEventListener("click", toggleLanguageMenu);
    languageMenuEl.addEventListener("click", chooseLanguage);
    dateChipEl.addEventListener("click", toggleDateChip);
    document.addEventListener("click", closeDateChip);
    document.addEventListener("click", closeCodeDateChips);
    document.addEventListener("click", closeLanguageMenu);
    document.addEventListener("click", closeMoreToolsMenu);
    document.addEventListener("keydown", closeLanguageMenuOnEscape);
    document.addEventListener("keydown", closeMoreToolsMenuOnEscape);
    document.addEventListener("keydown", closeTutorialOnEscape);
    window.addEventListener("resize", positionTutorial);
    window.addEventListener("scroll", positionTutorial, { passive: true });
    carouselCopyEl.addEventListener("click", copyCarouselCode);
    previousCodeEl.addEventListener("click", () => moveCarousel(-1, { includeUsed: true, carouselOnly: true }));
    nextCodeEl.addEventListener("click", () => moveCarousel(1, { preferUnused: true, carouselOnly: true }));
    reportPreviousEl.addEventListener("click", reportLastCopiedCode);
    let searchRenderTimer = 0;
    searchEl.addEventListener("input", () => {
      window.clearTimeout(searchRenderTimer);
      searchRenderTimer = window.setTimeout(render, 120);
    });
    archivePanelEl?.addEventListener("toggle", () => {
      if (archiveOpenSync) return;
      archivePinnedOpen = Boolean(archivePanelEl.open);
      renderArchivedExpiredCodes();
    });
    dialog.addEventListener("close", handleDialogClose);
    dialogCode.addEventListener("click", copyPendingReportCode);
    dialogCopyCode.addEventListener("click", copyPendingReportCode);
    expiredErrorInputs.forEach((input) => {
      input.addEventListener("change", updateExpiredDialogConfirm);
    });

    document.querySelectorAll(".tab").forEach((tab) => {
      tab.addEventListener("click", async () => {
        activeFilter = tab.dataset.filter;
        document.querySelectorAll(".tab").forEach((candidate) => {
          candidate.setAttribute("aria-selected", String(candidate === tab));
        });
        // Paint the selected tab before rebuilding the list (INP).
        await yieldForPaint();
        render();
      });
    });

    const showTutorialOnLoad = shouldShowFirstVisitTutorial();
    Promise.all([
      ensureLanguage(currentLanguage),
      ensureLanguage(getBrowserLanguage())
    ]).then(async () => {
      applyLanguage();
      const cachedCloudCodes = readCloudCodesCache();
      if (cachedCloudCodes) {
        applyFetchedCodeData(cachedCloudCodes, { renderCompare: false });
      }

      const live = await fetchCodesPayload();
      if (live && !isOlderCodesPayload(live)) {
        writeCloudCodesCache(live);
        applyFetchedCodeData(live, { renderCompare: false });
      } else if (!cachedCloudCodes) {
        render();
      }

      rememberCurrentBuild();
      if (showTutorialOnLoad) scheduleFirstVisitTutorial();
    }).catch(() => {
      render();
    }).finally(() => {
      revealCodesList();
    });
    codesUpdateRefreshEl.addEventListener("click", applyPendingCodesUpdate);
    initializeCaptchaWidgets();

    function initializeCaptchaWidgets() {
      if (!captchaEnabled) {
        captchaWidgets.forEach((widget) => { widget.hidden = true; });
        return;
      }

      captchaWidgets.forEach((widget) => { widget.hidden = false; });
    }

    function ensureTurnstileScript() {
      if (!captchaEnabled || window.turnstile || document.querySelector("script[data-turnstile-api]")) return;
      const s = document.createElement("script");
      s.src = "https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit";
      s.async = true;
      s.dataset.turnstileApi = "1";
      document.head.appendChild(s);
    }

    function destroyCaptchaWidget(widget) {
      if (!widget) return;
      const widgetId = captchaWidgetIds.get(widget);
      if (widgetId != null && window.turnstile?.remove) {
        try { window.turnstile.remove(widgetId); } catch {}
      }
      captchaWidgetIds.delete(widget);
      widget.replaceChildren();
    }

    function renderCaptchaWidget(widget) {
      if (!captchaEnabled || !widget) return true;
      if (captchaWidgetIds.has(widget)) return true;

      widget.hidden = false;
      if (!window.turnstile?.render) {
        captchaRenderAttempts += 1;
        return false;
      }

      const widgetId = window.turnstile.render(widget, {
        sitekey: turnstileSiteKey,
        theme: "dark",
        size: "flexible",
        appearance: "always",
        action: widget.dataset.captchaAction || "submit",
        "error-callback": () => {
          destroyCaptchaWidget(widget);
        }
      });
      captchaWidgetIds.set(widget, widgetId);
      return true;
    }

    function ensureCaptchaWidget(formEl) {
      if (!captchaEnabled || !formEl) return;

      const widget = formEl.querySelector(".captcha-widget");
      // Wait until the dialog/form is actually visible; Turnstile iframes break if mounted too early.
      const hostDialog = formEl.closest("dialog");
      if (hostDialog && !hostDialog.open) return;

      if (renderCaptchaWidget(widget)) return;

      if (captchaRenderAttempts <= 40) {
        window.setTimeout(() => ensureCaptchaWidget(formEl), 250);
      }
    }

    function scheduleCaptchaWidget(formEl) {
      if (!captchaEnabled) return;
      // Defer Turnstile until a submit/contact/report dialog opens.
      ensureTurnstileScript();
      const widget = formEl?.querySelector?.(".captcha-widget");
      destroyCaptchaWidget(widget);
      captchaRenderAttempts = 0;
      window.requestAnimationFrame(() => {
        window.requestAnimationFrame(() => ensureCaptchaWidget(formEl));
      });
    }

    function getCaptchaToken(formEl) {
      if (!captchaEnabled) return "";

      const widget = formEl.querySelector(".captcha-widget");
      if (!renderCaptchaWidget(widget) || !window.turnstile?.getResponse || !captchaWidgetIds.has(widget)) {
        showToast(t("captchaLoading"));
        return null;
      }

      const token = window.turnstile.getResponse(captchaWidgetIds.get(widget));
      if (!token) {
        showToast(t("captchaRequired"));
        return null;
      }

      return token;
    }

    function resetCaptcha(formEl) {
      if (!captchaEnabled) return;

      const widget = formEl?.querySelector?.(".captcha-widget");
      if (!widget || !captchaWidgetIds.has(widget)) return;
      if (window.turnstile?.reset) {
        try { window.turnstile.reset(captchaWidgetIds.get(widget)); } catch {
          destroyCaptchaWidget(widget);
        }
      }
    }

    function toggleDateChip(event) {
      event.stopPropagation();
      dateChipEl.classList.toggle("is-open");
    }

    function closeDateChip(event) {
      if (!dateChipEl.contains(event.target)) {
        dateChipEl.classList.remove("is-open");
      }
    }

    function closeCodeDateChips(event) {
      document.querySelectorAll(".code-date.is-open").forEach((chip) => {
        if (!chip.contains(event.target)) chip.classList.remove("is-open");
      });
    }

    function codeValue(entry) {
      return typeof entry === "string" ? entry : entry?.code;
    }

    function normalizeCodeEntry(entry) {
      const code = String(codeValue(entry) || "").trim().toUpperCase();
      if (!code) return null;

      const normalized = { code };
      if (entry && typeof entry === "object") {
        if (entry.addedAt) normalized.addedAt = entry.addedAt;
        if (entry.expiredAt) normalized.expiredAt = entry.expiredAt;
        if (entry.verifiedAt) normalized.verifiedAt = entry.verifiedAt;
      }
      return normalized;
    }

    function normalizeCodeList(entries) {
      if (!Array.isArray(entries)) return [];

      const seen = new Set();
      return entries
        .map(normalizeCodeEntry)
        .filter(Boolean)
        .filter((entry) => {
          if (seen.has(entry.code)) return false;
          seen.add(entry.code);
          return true;
        });
    }

    function applyCodeData(data) {
      codeEntries = normalizeCodeList(data?.active);
      archivedExpiredCodeEntries = normalizeCodeList(data?.expired);
      codes = codeEntries.map(codeValue);
      archivedExpiredCodes = archivedExpiredCodeEntries.map(codeValue);
      codeMetadataByCode = new Map(
        [...codeEntries, ...archivedExpiredCodeEntries].map((entry) => [codeValue(entry), entry])
      );
    }

    function applyCloudUpdatedAt(updatedAt) {
      if (!updatedAt || Number.isNaN(Date.parse(updatedAt))) return;
      dateChipEl.dataset.updatedAt = updatedAt;
      appliedCodesUpdatedAt = updatedAt;
      applyLanguage();
    }

    function readCloudCodesCache() {
      try {
        const cached = JSON.parse(sessionStorage.getItem(cloudCodesCacheKey));
        if (!cached || !Array.isArray(cached.active) || !Array.isArray(cached.expired)) return null;
        return cached;
      } catch {
        return null;
      }
    }

    function writeCloudCodesCache(data) {
      if (isOlderCodesPayload(data)) return;
      try {
        sessionStorage.setItem(cloudCodesCacheKey, JSON.stringify({
          updatedAt: data.updatedAt || null,
          active: data.active,
          expired: data.expired
        }));
      } catch {
        return;
      }
    }

    function resetCurrentIndex() {
      currentIndex = findNextUsableIndex(0);
    }

    function codesFingerprint(data) {
      const active = normalizeCodeList(data?.active).map(codeValue).sort();
      const expired = normalizeCodeList(data?.expired).map(codeValue).sort();
      return `a:${active.join(",")}|e:${expired.join(",")}`;
    }

    function codesPayloadTime(data) {
      const parsed = Date.parse(data?.updatedAt);
      return Number.isNaN(parsed) ? 0 : parsed;
    }

    function isOlderCodesPayload(data) {
      const incoming = codesPayloadTime(data);
      const current = codesPayloadTime({ updatedAt: appliedCodesUpdatedAt });
      return incoming > 0 && current > 0 && incoming < current;
    }

    function hideCodesUpdateBanner() {
      pendingCodesUpdate = null;
      codesUpdateBannerEl.hidden = true;
    }

    function showCodesUpdateBanner(data) {
      pendingCodesUpdate = data;
      codesUpdateBannerEl.hidden = false;
    }

    async function promptCodesOutOfDate(data = null) {
      const payload = data || await fetchCodesPayload();
      if (!payload || isOlderCodesPayload(payload)) return;
      if (codesFingerprint(payload) === appliedCodesFingerprint) return;
      showCodesUpdateBanner(payload);
    }

    function isCodesListRevealed() {
      return document.documentElement.classList.contains("codes-ready");
    }

    function revealCodesList() {
      if (codesClusterEl) {
        codesClusterEl.style.height = "";
        codesClusterEl.style.minHeight = "";
      }
      document.documentElement.classList.add("codes-ready");
    }

    function applyPendingCodesUpdate() {
      if (!pendingCodesUpdate) return;
      const data = pendingCodesUpdate;
      if (isOlderCodesPayload(data)) {
        hideCodesUpdateBanner();
        return;
      }
      writeCloudCodesCache(data);
      applyFetchedCodeData(data);
    }

    function applyFetchedCodeData(data, options = {}) {
      if (isOlderCodesPayload(data)) return;
      const { renderCompare = true } = options;
      const previousCodeSet = new Set(codes);
      const currentCode = currentIndex === -1 ? null : codes[currentIndex];
      const previousUnused = codes.filter((code) => getStatus(code) === "unused");
      const wasOnFirstUnused = Boolean(currentCode) && previousUnused[0] === currentCode;

      applyCodeData(data);
      applyCloudUpdatedAt(data.updatedAt);
      appliedCodesFingerprint = codesFingerprint(data);
      if (data.updatedAt && !Number.isNaN(Date.parse(data.updatedAt))) {
        appliedCodesUpdatedAt = data.updatedAt;
      }
      hideCodesUpdateBanner();

      const unusedCodes = getUnusedCodes();
      // Codes that exist in live data but not in the previous list (empty boot or session cache).
      const newlyAppearedUnused = unusedCodes.filter((code) => !previousCodeSet.has(code));
      visitCarouselCodes = unusedCodes.slice();

      if (newlyAppearedUnused.length && (wasOnFirstUnused || !lastCopiedCode || !currentCode)) {
        // Live hydration added unused codes — don't keep an empty or cached carousel selection.
        currentIndex = codes.indexOf(newlyAppearedUnused[0]);
      } else if (currentCode && codes.includes(currentCode) && getStatus(currentCode) !== "expired") {
        currentIndex = codes.indexOf(currentCode);
      } else {
        resetCurrentIndex();
      }

      render();
      if (renderCompare && compareDialog.open) renderCompareResults();
    }

    async function fetchCodesPayload() {
      const endpoints = [];
      if (publicCodesUrl) endpoints.push(publicCodesUrl);
      endpoints.push(codesEndpoint);

      for (const endpoint of endpoints) {
        try {
          const response = await fetch(endpoint, { cache: "no-store" });
          if (!response.ok) continue;

          const data = await response.json();
          if (!Array.isArray(data?.active) || !Array.isArray(data?.expired)) continue;
          return data;
        } catch {
          continue;
        }
      }

      return null;
    }

    async function loadCloudCodes() {
      const data = await fetchCodesPayload();
      if (!data) return null;
      if (isOlderCodesPayload(data)) return data;

      writeCloudCodesCache(data);

      if (!isCodesListRevealed()) {
        applyFetchedCodeData(data);
        return data;
      }

      if (codesFingerprint(data) !== appliedCodesFingerprint) {
        applyCodeData(data);
        showCodesUpdateBanner(data);
      }
      return data;
    }

    function codeAddedAt(code) {
      return codeMetadataByCode.get(code)?.addedAt || null;
    }

    function codeExpiredAt(code) {
      return codeMetadataByCode.get(code)?.expiredAt || getCodeState(code).reportedAt || null;
    }

    function codeVerifiedAt(code) {
      const meta = codeMetadataByCode.get(code);
      if (!meta) return null;
      if (meta.verifiedAt) return meta.verifiedAt;
      // Match backend backfill: active codes without verifiedAt use addedAt.
      if (!meta.expiredAt && meta.addedAt) return meta.addedAt;
      return null;
    }

    function isNewCode(code) {
      if (!previousSeenBuildAt) return false;
      // Used status must not move a new code out of its current list section.
      if (getStatus(code) === "expired") return false;
      const addedAt = codeAddedAt(code);
      if (!addedAt) return false;
      return Date.parse(addedAt) > Date.parse(previousSeenBuildAt);
    }

    function formatMinorDate(value) {
      const date = new Date(value);
      if (Number.isNaN(date.getTime())) return "";
      return new Intl.DateTimeFormat(undefined, {
        month: "numeric",
        day: "2-digit"
      }).format(date);
    }

    function formatLocalStamp(value) {
      const date = new Date(value);
      if (Number.isNaN(date.getTime())) return "";
      const parts = new Intl.DateTimeFormat(undefined, {
        month: "2-digit",
        day: "2-digit",
        hour: "2-digit",
        minute: "2-digit",
        hourCycle: "h23"
      }).formatToParts(date);
      const pick = (type) => parts.find((part) => part.type === type)?.value || "";
      return `${pick("month")}/${pick("day")} at ${pick("hour")}:${pick("minute")}`;
    }

    function syncDateChip(chip, code, type = "added") {
      if (!chip) return false;
      const addedAt = codeAddedAt(code);
      if (!addedAt) return false;
      const expiredAt = codeExpiredAt(code);
      const verifiedAt = codeVerifiedAt(code);
      const isExpiredChip = type === "expired" || chip.classList.contains("expired-date");
      const dateValue = isExpiredChip ? expiredAt : addedAt;
      if (!dateValue) return false;
      chip.className = `code-date${isExpiredChip ? " expired-date" : ""}`;
      chip.textContent = formatMinorDate(dateValue);
      const baseTooltip = isExpiredChip
        ? t("expiredTooltip", {
            addedAt: formatLocalStamp(addedAt),
            expiredAt: formatLocalStamp(expiredAt)
          })
        : t("addedTooltip", {
            addedAt: formatLocalStamp(addedAt)
          });
      chip.dataset.tooltip = verifiedAt
        ? `${baseTooltip}${t("verifiedTooltipPart", { verifiedAt: formatLocalStamp(verifiedAt) })}`
        : baseTooltip;
      chip.title = chip.dataset.tooltip;
      chip.setAttribute("aria-label", chip.dataset.tooltip);
      return true;
    }

    function bindDateChipToggle(chip) {
      if (!chip || chip.dataset.bound === "1") return;
      chip.dataset.bound = "1";
      chip.addEventListener("click", (event) => {
        event.stopPropagation();
        const isOpen = chip.classList.contains("is-open");
        document.querySelectorAll(".code-date.is-open").forEach((candidate) => candidate.classList.remove("is-open"));
        chip.classList.toggle("is-open", !isOpen);
      });
    }

    function createDateChip(code, type = "added") {
      const chip = document.createElement("button");
      chip.type = "button";
      if (!syncDateChip(chip, code, type)) return null;
      bindDateChipToggle(chip);
      return chip;
    }

    function loadLastSeenBuild() {
      try {
        return localStorage.getItem(lastSeenStorageKey) || "";
      } catch {
        return "";
      }
    }

    function rememberCurrentBuild() {
      const updatedAt = dateChipEl.dataset.updatedAt;
      if (!updatedAt || Number.isNaN(Date.parse(updatedAt))) return;
      const previousLastSeen = loadLastSeenBuild();
      if (previousLastSeen && Date.parse(updatedAt) < Date.parse(previousLastSeen)) return;
      try {
        localStorage.setItem(lastSeenStorageKey, updatedAt);
      } catch {
        return;
      }
    }

    function loadLanguage() {
      try {
        const savedLanguage = localStorage.getItem(languageStorageKey);
        return supportedLanguages.includes(savedLanguage) ? savedLanguage : getBrowserLanguage();
      } catch {
        return getBrowserLanguage();
      }
    }

    function getBrowserLanguage() {
      const browserLanguages = navigator.languages && navigator.languages.length
        ? navigator.languages
        : [navigator.language || "en"];

      for (const browserLanguage of browserLanguages) {
        const normalized = browserLanguage.toLowerCase();
        const primaryLanguage = normalized.split("-")[0];
        if (supportedLanguages.includes(normalized)) return normalized;
        if (supportedLanguages.includes(primaryLanguage)) return primaryLanguage;
      }

      return "en";
    }

    function saveLanguage() {
      try {
        localStorage.setItem(languageStorageKey, currentLanguage);
      } catch {
        return;
      }
    }

    function t(key, values = {}) {
      const catalog = translations[currentLanguage] || translations.en;
      const template = catalog[key] || translations.en[key] || key;
      return Object.entries(values).reduce((text, [name, value]) => {
        return text.replaceAll(`{${name}}`, value);
      }, template);
    }

    function ensureLanguage(lang) {
      if (!lang || lang === "en" || translations[lang]) return Promise.resolve();
      if (!supportedLanguages.includes(lang)) return Promise.resolve();
      if (translationLoads[lang]) return translationLoads[lang];

      translationLoads[lang] = fetch(`/i18n/${encodeURIComponent(lang)}.json`)
        .then((response) => {
          if (!response.ok) throw new Error(`Failed to load language ${lang}`);
          return response.json();
        })
        .then((overrides) => {
          translations[lang] = { ...translations.en, ...overrides };
        })
        .catch((error) => {
          delete translationLoads[lang];
          console.warn(error);
          if (currentLanguage === lang) currentLanguage = "en";
        });

      return translationLoads[lang];
    }

    function getLocalUpdatedAt() {
      const updatedAt = new Date(dateChipEl.dataset.updatedAt);
      if (Number.isNaN(updatedAt.getTime())) return "__LAST_UPDATED_LOCAL__";
      return new Intl.DateTimeFormat(undefined, {
        month: "numeric",
        day: "numeric",
        hour: "numeric",
        minute: "2-digit",
        timeZoneName: "short"
      }).format(updatedAt);
    }

    function applyLanguage() {
      document.documentElement.lang = languageHtmlLang[currentLanguage] || "en";
      document.documentElement.dir = languageDirections[currentLanguage] || "ltr";
      document.querySelectorAll("[data-i18n]").forEach((element) => {
        element.textContent = t(element.dataset.i18n);
      });
      document.querySelectorAll("[data-i18n-placeholder]").forEach((element) => {
        element.placeholder = t(element.dataset.i18nPlaceholder);
      });

      const lastUpdated = t("lastUpdated", { updatedAt: getLocalUpdatedAt() });
      dateChipEl.dataset.tooltip = lastUpdated;
      dateChipEl.setAttribute("aria-label", lastUpdated);
      dateChipEl.title = lastUpdated;

      updateLanguageMenu();
      tutorialHelpEl.setAttribute("aria-label", t("tutorialHelpTitle"));
      tutorialHelpEl.title = t("tutorialHelpTitle");
      contactButtonEl.setAttribute("aria-label", t("contact"));
      contactButtonEl.title = t("contact");
      compareCodesButton.setAttribute("aria-label", t("compareTitle"));
      compareCodesButton.title = t("compareTitle");
      compareMarkUsedEl.setAttribute("aria-label", t("compareMarkUsedTitle"));
      compareMarkUsedEl.title = t("compareMarkUsedTitle");
      compareCopyButtons.new.setAttribute("aria-label", t("compareCopyNew"));
      compareCopyButtons.new.title = t("compareCopyNew");
      compareCopyButtons.matched.setAttribute("aria-label", t("compareCopyMatched"));
      compareCopyButtons.matched.title = t("compareCopyMatched");
      compareCopyButtons.expired.setAttribute("aria-label", t("compareCopyExpired"));
      compareCopyButtons.expired.title = t("compareCopyExpired");
      compareCopyButtons.unknown.setAttribute("aria-label", t("compareCopyMissing"));
      compareCopyButtons.unknown.title = t("compareCopyMissing");
      moreToolsButtonEl.setAttribute("aria-label", t("moreToolsTitle"));
      moreToolsButtonEl.title = t("moreToolsTitle");
      copyAllEl.setAttribute("aria-label", t("copyAllTitle"));
      copyAllEl.title = t("copyAllTitle");
      resetUsedEl.setAttribute("aria-label", t("resetTitle"));
      resetUsedEl.title = t("resetTitle");
      previousCodeEl.setAttribute("aria-label", t("previousTitle"));
      previousCodeEl.title = t("previousTitle");
      nextCodeEl.setAttribute("aria-label", t("nextTitle"));
      nextCodeEl.title = t("nextTitle");
      dialogCopyCode.setAttribute("aria-label", t("copyCode"));
      dialogCopyCode.title = t("copyCode");
      if (compareDialog.open) renderCompareResults();
      if (!tutorialDialog.hidden) setTutorialStep(tutorialStepIndex);
      document.querySelectorAll(".code-card").forEach((card) => {
        const code = card.dataset.code;
        if (!code) return;
        card.querySelectorAll(".code-date").forEach((chip) => {
          syncDateChip(chip, code, chip.classList.contains("expired-date") ? "expired" : "added");
        });
      });
    }

    function updateLanguageMenu() {
      languageButtonEl.querySelector("span").textContent = languageButtonLabels[currentLanguage] || "EN";
      languageButtonEl.setAttribute("aria-label", t("languageLabel"));
      languageButtonEl.title = t("languageLabel");
      languageMenuEl.querySelectorAll("[data-language]").forEach((option) => {
        const selected = option.dataset.language === currentLanguage;
        option.classList.toggle("is-selected", selected);
        option.setAttribute("aria-checked", String(selected));
      });
    }

    function toggleLanguageMenu(event) {
      event.stopPropagation();
      closeMoreToolsMenu();
      const shouldOpen = languageMenuEl.hidden;
      languageMenuEl.hidden = !shouldOpen;
      languageButtonEl.setAttribute("aria-expanded", String(shouldOpen));
    }

    function closeLanguageMenu(event) {
      if (event && languagePickerEl.contains(event.target)) return;
      languageMenuEl.hidden = true;
      languageButtonEl.setAttribute("aria-expanded", "false");
    }

    function closeLanguageMenuOnEscape(event) {
      if (event.key === "Escape") closeLanguageMenu();
    }

    function toggleMoreToolsMenu(event) {
      event.stopPropagation();
      closeLanguageMenu();
      const shouldOpen = moreToolsMenuEl.hidden;
      moreToolsMenuEl.hidden = !shouldOpen;
      moreToolsButtonEl.setAttribute("aria-expanded", String(shouldOpen));
    }

    function closeMoreToolsMenu(event) {
      if (event && moreToolsPickerEl.contains(event.target)) return;
      moreToolsMenuEl.hidden = true;
      moreToolsButtonEl.setAttribute("aria-expanded", "false");
    }

    function closeMoreToolsMenuOnEscape(event) {
      if (event.key === "Escape") closeMoreToolsMenu();
    }

    function chooseMoreTool(event) {
      const option = event.target.closest("[data-more-action]");
      if (!option) return;
      event.stopPropagation();
      closeMoreToolsMenu();
      const action = option.dataset.moreAction;
      if (action === "compare") openCompareDialog();
      else if (action === "export") copyAllCodes();
      else if (action === "reset") resetState();
    }

    async function chooseLanguage(event) {
      const option = event.target.closest("[data-language]");
      if (!option) return;
      event.stopPropagation();
      currentLanguage = supportedLanguages.includes(option.dataset.language) ? option.dataset.language : "en";
      saveLanguage();
      closeLanguageMenu();
      await ensureLanguage(currentLanguage);
      applyLanguage();
      render();
    }

    newCodeForm.addEventListener("submit", submitNewCode);
    document.querySelector("#new-code-cancel").addEventListener("click", () => newCodeDialog.close());
    newCodeInput.addEventListener("input", () => {
      newCodeInput.value = newCodeInput.value.toUpperCase().replace(/\s+/g, "");
      refreshNewCodeValidation();
    });
    newCodeCheckedCheck.addEventListener("change", refreshNewCodeValidation);
    contactForm.addEventListener("submit", submitContactMessage);
    document.querySelector("#contact-cancel").addEventListener("click", () => contactDialog.close());
    contactMessage.addEventListener("input", () => {
      contactSubmit.disabled = !contactMessage.value.trim();
    });
    tutorialSkipEl.addEventListener("click", completeTutorial);
    tutorialPreviousEl.addEventListener("click", () => setTutorialStep(tutorialStepIndex - 1));
    tutorialNextEl.addEventListener("click", advanceTutorial);

    function shouldShowFirstVisitTutorial() {
      try {
        return !localStorage.getItem(tutorialStorageKey) &&
          !localStorage.getItem(storageKey) &&
          !localStorage.getItem(languageStorageKey) &&
          !localStorage.getItem(lastSeenStorageKey);
      } catch {
        return false;
      }
    }

    function markTutorialSeen() {
      try {
        localStorage.setItem(tutorialStorageKey, new Date().toISOString());
      } catch {
        return;
      }
    }

    function scheduleFirstVisitTutorial() {
      const start = () => {
        if (!tutorialDialog.hidden || tutorialHintVisible) return;
        markTutorialSeen();
        showTutorialHint();
      };
      const delayMs = 2200;
      if (typeof requestIdleCallback === "function") {
        requestIdleCallback(() => setTimeout(start, delayMs), { timeout: delayMs + 2000 });
        return;
      }
      setTimeout(start, delayMs);
    }

    function showTutorialHint() {
      tutorialHelpEl.setAttribute("data-tooltip", t("tutorialHintMessage"));
      tutorialHelpEl.closest(".actions")?.classList.add("is-tutorial-hint-open");
      tutorialHelpEl.classList.add("tutorial-hint-active");
      tutorialHintVisible = true;
      document.addEventListener("click", dismissTutorialHintOnOutsideClick);
      window.setTimeout(dismissTutorialHint, 8000);
    }

    function dismissTutorialHint() {
      if (!tutorialHintVisible) return;
      tutorialHintVisible = false;
      tutorialHelpEl.classList.remove("tutorial-hint-active");
      tutorialHelpEl.closest(".actions")?.classList.remove("is-tutorial-hint-open");
      document.removeEventListener("click", dismissTutorialHintOnOutsideClick);
    }

    function dismissTutorialHintOnOutsideClick(event) {
      if (event.target.closest("#tutorial-help")) return;
      dismissTutorialHint();
    }

    function openTutorial() {
      markTutorialSeen();
      document.body.classList.add("tutorial-active");
      tutorialDialog.hidden = false;
      setTutorialStep(0);
      tutorialNextEl.focus();
    }

    function setTutorialStep(index) {
      tutorialStepIndex = Math.min(Math.max(index, 0), tutorialSteps.length - 1);
      const step = tutorialSteps[tutorialStepIndex];
      tutorialTitleEl.textContent = t(step.titleKey);
      if (step.messageHtmlKey) {
        tutorialMessageEl.innerHTML = t(step.messageHtmlKey);
      } else {
        tutorialMessageEl.textContent = step.messageKey
          ? t(step.messageKey)
          : (typeof step.message === "function" ? step.message() : step.message);
      }
      tutorialPreviousEl.disabled = tutorialStepIndex === 0;
      tutorialSkipEl.textContent = t("tutorialSkip");
      tutorialPreviousEl.textContent = t("tutorialBack");
      tutorialNextEl.textContent = tutorialStepIndex === tutorialSteps.length - 1 ? t("tutorialDone") : t("tutorialNext");
      tutorialDotsEl.innerHTML = tutorialSteps.map((_, dotIndex) => {
        const activeClass = dotIndex === tutorialStepIndex ? " is-active" : "";
        return `<span class="tutorial-dot${activeClass}"></span>`;
      }).join("");
      highlightTutorialTarget();
    }

    async function advanceTutorial() {
      // Let the Next button paint before scroll/layout work (INP).
      await yieldForPaint();
      if (tutorialStepIndex === tutorialSteps.length - 1) {
        completeTutorial();
        return;
      }
      setTutorialStep(tutorialStepIndex + 1);
    }

    function completeTutorial() {
      markTutorialSeen();
      clearTutorialHighlight();
      tutorialDialog.hidden = true;
      document.body.classList.remove("tutorial-active");
      window.scrollTo({ top: 0, left: 0, behavior: "smooth" });
    }

    function closeTutorialOnEscape(event) {
      if (event.key === "Escape" && !tutorialDialog.hidden) completeTutorial();
    }

    function getTutorialTarget() {
      const selector = tutorialSteps[tutorialStepIndex]?.target;
      return selector ? document.querySelector(selector) : null;
    }

    function isVisibleTutorialTarget(element) {
      if (!element) return false;
      const rect = element.getBoundingClientRect();
      const styles = window.getComputedStyle(element);
      return rect.width > 0 &&
        rect.height > 0 &&
        styles.display !== "none" &&
        styles.visibility !== "hidden";
    }

    function getTutorialHighlightTargets() {
      const step = tutorialSteps[tutorialStepIndex] || {};
      const selectors = step.highlightTargets || [step.target];
      return selectors
        .flatMap((selector) => selector ? [...document.querySelectorAll(selector)] : [])
        .filter(isVisibleTutorialTarget);
    }

    function positionTutorialBoundary() {
      if (!tutorialBoundaryEl || tutorialDialog.hidden) return;
      const targets = getTutorialHighlightTargets();
      if (!targets.length) {
        tutorialBoundaryEl.hidden = true;
        return;
      }
      const padding = 7;
      const rects = targets.map((element) => element.getBoundingClientRect());
      const left = Math.min(...rects.map((rect) => rect.left)) - padding;
      const top = Math.min(...rects.map((rect) => rect.top)) - padding;
      const right = Math.max(...rects.map((rect) => rect.right)) + padding;
      const bottom = Math.max(...rects.map((rect) => rect.bottom)) + padding;
      tutorialBoundaryEl.hidden = false;
      tutorialBoundaryEl.style.left = `${left}px`;
      tutorialBoundaryEl.style.top = `${top}px`;
      tutorialBoundaryEl.style.width = `${right - left}px`;
      tutorialBoundaryEl.style.height = `${bottom - top}px`;
    }

    function clearTutorialHighlight() {
      tutorialTargetEls.forEach((element) => {
        element.classList.remove("tutorial-highlight");
        element.classList.remove("tutorial-highlight-hotbar");
        element.classList.remove("tutorial-highlight-filters");
        element.classList.remove("tutorial-highlight-danger");
      });
      tutorialTargetEls = [];
      if (tutorialBoundaryEl) tutorialBoundaryEl.hidden = true;
    }

    function highlightTutorialTarget() {
      clearTutorialHighlight();
      const scrollTarget = getTutorialTarget();
      tutorialTargetEls = getTutorialHighlightTargets();
      if (!scrollTarget || !tutorialTargetEls.length) {
        positionTutorial();
        return;
      }
      tutorialTargetEls.forEach((element) => {
        element.classList.add("tutorial-highlight");
        if (tutorialSteps[tutorialStepIndex]?.highlightClass) {
          element.classList.add(tutorialSteps[tutorialStepIndex].highlightClass);
        }
      });
      scrollTarget.scrollIntoView({ block: "center", inline: "nearest" });
      requestAnimationFrame(positionTutorial);
      window.setTimeout(positionTutorial, 80);
    }

    function positionTutorial() {
      if (tutorialDialog.hidden) return;
      const target = getTutorialTarget();
      const viewportPadding = 12;
      const gap = 14;
      tutorialDialog.style.maxHeight = "";
      const dialogRect = tutorialDialog.getBoundingClientRect();
      const dialogWidth = dialogRect.width;
      const dialogHeight = dialogRect.height;

      if (!target) {
        tutorialDialog.style.left = `${Math.max(viewportPadding, (window.innerWidth - dialogWidth) / 2)}px`;
        tutorialDialog.style.top = `${Math.max(viewportPadding, (window.innerHeight - dialogHeight) / 2)}px`;
        positionTutorialBoundary();
        return;
      }

      const targetRect = target.getBoundingClientRect();
      const preferredPlacement = tutorialSteps[tutorialStepIndex]?.placement || "bottom";
      const spaceAbove = targetRect.top - gap - viewportPadding;
      const spaceBelow = window.innerHeight - targetRect.bottom - gap - viewportPadding;
      const centerLeft = targetRect.left + targetRect.width / 2 - dialogWidth / 2;
      const centerTop = targetRect.top + targetRect.height / 2 - dialogHeight / 2;
      const candidates = [
        {
          placement: "bottom",
          left: centerLeft,
          top: targetRect.bottom + gap
        },
        {
          placement: "top",
          left: centerLeft,
          top: targetRect.top - dialogHeight - gap
        },
        {
          placement: "right",
          left: targetRect.right + gap,
          top: centerTop
        },
        {
          placement: "left",
          left: targetRect.left - dialogWidth - gap,
          top: centerTop
        }
      ];

      const clamp = (value, min, max) => Math.min(Math.max(value, min), Math.max(min, max));
      const rectsOverlap = (a, b) => (
        a.left < b.right &&
        a.right > b.left &&
        a.top < b.bottom &&
        a.bottom > b.top
      );
      const scoreCandidate = (candidate) => {
        const maxLeft = window.innerWidth - dialogWidth - viewportPadding;
        const maxTop = window.innerHeight - dialogHeight - viewportPadding;
        const left = clamp(candidate.left, viewportPadding, maxLeft);
        const top = clamp(candidate.top, viewportPadding, maxTop);
        const rect = {
          left,
          top,
          right: left + dialogWidth,
          bottom: top + dialogHeight
        };
        const offscreenPenalty =
          Math.abs(left - candidate.left) +
          Math.abs(top - candidate.top);
        const overlaps = rectsOverlap(rect, targetRect);
        const overlapPenalty = overlaps ? 10000 : 0;
        const preferencePenalty = candidate.placement === preferredPlacement ? 0 : 120;
        return {
          left,
          top,
          overlaps,
          score: overlapPenalty + offscreenPenalty + preferencePenalty
        };
      };

      const best = candidates
        .map(scoreCandidate)
        .sort((a, b) => a.score - b.score)[0];

      if (best.overlaps && Math.max(spaceAbove, spaceBelow) > 96) {
        const placeBelow = spaceBelow >= spaceAbove;
        const availableHeight = Math.max(96, placeBelow ? spaceBelow : spaceAbove);
        tutorialDialog.style.maxHeight = `${availableHeight}px`;
        const resizedRect = tutorialDialog.getBoundingClientRect();
        const maxLeft = window.innerWidth - resizedRect.width - viewportPadding;
        const left = clamp(
          targetRect.left + targetRect.width / 2 - resizedRect.width / 2,
          viewportPadding,
          maxLeft
        );
        const top = placeBelow
          ? targetRect.bottom + gap
          : targetRect.top - resizedRect.height - gap;
        tutorialDialog.style.left = `${left}px`;
        tutorialDialog.style.top = `${Math.max(viewportPadding, top)}px`;
        positionTutorialBoundary();
        return;
      }

      tutorialDialog.style.left = `${best.left}px`;
      tutorialDialog.style.top = `${best.top}px`;
      positionTutorialBoundary();
    }

    function loadState() {
      try {
        return JSON.parse(localStorage.getItem(storageKey)) || {};
      } catch {
        return {};
      }
    }

    function saveState() {
      try {
        localStorage.setItem(storageKey, JSON.stringify(state));
      } catch {
        return;
      }
    }

    function syncStateFromStorage() {
      try {
        const storedState = JSON.parse(localStorage.getItem(storageKey)) || {};
        Object.keys(state).forEach((key) => {
          if (!storedState[key]) delete state[key];
        });
        Object.assign(state, storedState);
      } catch {
        return;
      }
    }

    function getSubmittedCodes() {
      try {
        const savedCodes = JSON.parse(localStorage.getItem(submittedCodeStorageKey)) || [];
        return new Set(savedCodes.map((code) => String(code).trim().toUpperCase()).filter(Boolean));
      } catch {
        return new Set();
      }
    }

    function hasSubmittedCode(code) {
      return getSubmittedCodes().has(code);
    }

    function rememberSubmittedCode(code) {
      try {
        const submittedCodes = getSubmittedCodes();
        submittedCodes.add(code);
        localStorage.setItem(submittedCodeStorageKey, JSON.stringify([...submittedCodes]));
      } catch {
        return;
      }
    }

    function createBrowserSubmissionId() {
      if (window.crypto?.randomUUID) return `browser-${window.crypto.randomUUID()}`;

      const bytes = new Uint8Array(16);
      if (window.crypto?.getRandomValues) {
        window.crypto.getRandomValues(bytes);
      } else {
        for (let index = 0; index < bytes.length; index += 1) {
          bytes[index] = Math.floor(Math.random() * 256);
        }
      }

      return `browser-${[...bytes].map((byte) => byte.toString(16).padStart(2, "0")).join("")}`;
    }

    function getBrowserSubmissionId() {
      if (browserSubmissionIdFallback) return browserSubmissionIdFallback;

      try {
        const savedId = localStorage.getItem(browserSubmissionIdStorageKey);
        if (savedId) {
          browserSubmissionIdFallback = savedId;
          ensureBrowserSubmissionIdCreatedAt({ backfillExisting: true });
          return browserSubmissionIdFallback;
        }

        browserSubmissionIdFallback = createBrowserSubmissionId();
        localStorage.setItem(browserSubmissionIdStorageKey, browserSubmissionIdFallback);
        ensureBrowserSubmissionIdCreatedAt();
        return browserSubmissionIdFallback;
      } catch {
        browserSubmissionIdFallback = createBrowserSubmissionId();
        browserSubmissionIdCreatedAtFallback = new Date().toISOString();
        return browserSubmissionIdFallback;
      }
    }

    function ensureBrowserSubmissionIdCreatedAt(options = {}) {
      if (browserSubmissionIdCreatedAtFallback) return browserSubmissionIdCreatedAtFallback;

      try {
        const savedCreatedAt = localStorage.getItem(browserSubmissionIdCreatedAtStorageKey);
        if (savedCreatedAt) {
          browserSubmissionIdCreatedAtFallback = savedCreatedAt;
          return browserSubmissionIdCreatedAtFallback;
        }

        const createdAtDate = options.backfillExisting
          ? new Date(Date.now() - establishedBrowserIdBackfillMs)
          : new Date();
        browserSubmissionIdCreatedAtFallback = createdAtDate.toISOString();
        localStorage.setItem(browserSubmissionIdCreatedAtStorageKey, browserSubmissionIdCreatedAtFallback);
        return browserSubmissionIdCreatedAtFallback;
      } catch {
        browserSubmissionIdCreatedAtFallback = browserSubmissionIdCreatedAtFallback || new Date().toISOString();
        return browserSubmissionIdCreatedAtFallback;
      }
    }

    function getBrowserSubmissionIdCreatedAt() {
      getBrowserSubmissionId();
      return ensureBrowserSubmissionIdCreatedAt();
    }

    function getCodeState(code) {
      return state[code] || {};
    }

    function setCodeState(code, patch, options = {}) {
      state[code] = {
        ...getCodeState(code),
        ...patch,
        updatedAt: new Date().toISOString()
      };
      saveState();
      if (!options.skipRender) render();
    }

    function clearCodeState(code) {
      delete state[code];
      saveState();
    }

    function getStatus(code) {
      const codeState = getCodeState(code);
      if (codeState.report === "expired") return "expired";
      if (codeState.used) return "used";
      return "unused";
    }

    function getListedCodeStatus(code) {
      if (archivedExpiredCodes.includes(code)) return "expired";
      if (codes.includes(code)) return "active";
      return null;
    }

    function getUnusedCodes() {
      return codes.filter((code) => getStatus(code) === "unused");
    }

    function getVisitCarouselCodes() {
      visitCarouselCodes = visitCarouselCodes.filter((code) => codes.includes(code) && getStatus(code) !== "expired");
      const queuedCodes = new Set(visitCarouselCodes);
      getUnusedCodes().forEach((code) => {
        if (!queuedCodes.has(code)) visitCarouselCodes.push(code);
      });
      return visitCarouselCodes;
    }

    function findNextUsableIndex(startIndex) {
      const firstUnused = codes.findIndex((code) => getStatus(code) === "unused");
      if (firstUnused !== -1) return firstUnused;
      const firstActive = codes.findIndex((code) => getStatus(code) !== "expired");
      return firstActive === -1 ? 0 : firstActive;
    }

    function render() {
      syncStateFromStorage();
      const query = searchEl.value.trim().toUpperCase();
      const visibleCodes = codes.filter((code) => {
        const status = getStatus(code);
        const matchesQuery = !query || code.includes(query);
        const matchesFilter =
          activeFilter === "all" ||
          (activeFilter === "unused" && status === "unused") ||
          (activeFilter === "used" && (status === "used" || status === "expired")) ||
          activeFilter === status;
        return matchesQuery && matchesFilter;
      });

      const visibleNewCodes = visibleCodes.filter(isNewCode);
      const visibleNormalCodes = visibleCodes.filter((code) => !isNewCode(code));

      syncCodeCards(newCodesEl, visibleNewCodes, { isNew: true });
      newHeadingEl.style.display = visibleNewCodes.length ? "" : "none";
      newCodesEl.style.display = visibleNewCodes.length ? "" : "none";

      syncCodeCards(codesEl, visibleNormalCodes, {});
      emptyEl.style.display = visibleCodes.length ? "none" : "block";
      renderArchivedExpiredCodes();
      renderCarousel();
      renderProgress();
    }

    function syncCodeCards(container, codeList, options = {}) {
      const existingCards = [...container.querySelectorAll(".code-card[data-code]")];
      const byCode = new Map(existingCards.map((card) => [card.dataset.code, card]));
      const sameOrder =
        existingCards.length === codeList.length &&
        existingCards.every((card, index) => card.dataset.code === codeList[index]);

      if (sameOrder) {
        codeList.forEach((code) => {
          const card = byCode.get(code);
          hydrateCodeCard(card);
          patchCodeCard(card, code);
          card.classList.toggle("is-new", Boolean(options.isNew));
        });
        return;
      }

      const fragment = document.createDocumentFragment();
      codeList.forEach((code) => {
        let card = byCode.get(code);
        if (card) {
          hydrateCodeCard(card);
          patchCodeCard(card, code);
          card.classList.toggle("is-new", Boolean(options.isNew));
          fragment.appendChild(card);
          byCode.delete(code);
        } else {
          fragment.appendChild(createCodeCard(code, options));
        }
      });
      container.replaceChildren(fragment);
    }

    function hydrateCodeCard(card) {
      if (!card || card.dataset.hydrated === "1") return;
      const code = card.dataset.code;
      if (!code) return;

      const input = card.querySelector(".code-field");
      if (input) input.addEventListener("focus", () => input.select());

      const copyButton = card.querySelector(".list-copy");
      if (copyButton) copyButton.addEventListener("click", () => copyCode(code));

      const usedButton = card.querySelector(".list-action.used");
      if (usedButton) usedButton.addEventListener("click", () => toggleCodeUsed(code));

      const expiredButton = card.querySelector(".list-action.expired");
      if (expiredButton) {
        expiredButton.addEventListener("click", () => {
          openReportDialog(code, getStatus(code) === "expired" ? "unexpired" : "expired");
        });
      }

      const actions = card.querySelector(".list-actions");
      if (actions && !actions.querySelector(".code-date")) {
        const addedDateChip = createDateChip(code, "added");
        const expiredDateChip = getStatus(code) === "expired" ? createDateChip(code, "expired") : null;
        if (addedDateChip) actions.appendChild(addedDateChip);
        if (expiredDateChip) actions.appendChild(expiredDateChip);
      } else {
        card.querySelectorAll(".code-date").forEach((chip) => {
          syncDateChip(chip, code, chip.classList.contains("expired-date") ? "expired" : "added");
          bindDateChipToggle(chip);
        });
      }

      card.dataset.hydrated = "1";
    }

    function updateCurrentCardHighlight() {
      const currentCode = currentIndex === -1 ? null : codes[currentIndex];
      document.querySelectorAll(".code-card.is-current").forEach((card) => {
        card.classList.remove("is-current");
      });
      if (!currentCode) return;
      findCodeCard(currentCode)?.classList.add("is-current");
    }

    function renderCarouselOnly() {
      renderCarousel();
      updateCurrentCardHighlight();
    }

    function codeMatchesCurrentView(code) {
      const status = getStatus(code);
      const query = searchEl.value.trim().toUpperCase();
      const matchesQuery = !query || code.includes(query);
      return matchesQuery && (
        activeFilter === "all" ||
        (activeFilter === "unused" && status === "unused") ||
        (activeFilter === "used" && (status === "used" || status === "expired")) ||
        activeFilter === status
      );
    }

    function findCodeCard(code) {
      const escaped = typeof CSS !== "undefined" && CSS.escape ? CSS.escape(code) : code.replace(/"/g, '\\"');
      return document.querySelector(`.code-card[data-code="${escaped}"]`);
    }

    function updateListEmptyState() {
      const hasVisible = Boolean(codesEl.querySelector(".code-card") || newCodesEl.querySelector(".code-card"));
      emptyEl.style.display = hasVisible ? "none" : "block";
      const hasNew = Boolean(newCodesEl.querySelector(".code-card"));
      newHeadingEl.style.display = hasNew ? "" : "none";
      newCodesEl.style.display = hasNew ? "" : "none";
    }

    function patchCodeCard(card, code) {
      const status = getStatus(code);
      card.classList.remove("is-unused", "is-used", "is-expired");
      card.classList.add(`is-${status}`);
      card.classList.toggle("is-current", codes[currentIndex] === code);

      const badge = card.querySelector(".meta > .badge");
      if (badge) {
        badge.className = `badge ${status}`;
        badge.textContent = getStatusLabel(status);
      }

      const copyButton = card.querySelector(".list-copy");
      if (copyButton) copyButton.disabled = status === "expired";

      const usedButton = card.querySelector(".list-action.used");
      if (usedButton) {
        const isUsed = status === "used";
        usedButton.disabled = status === "expired";
        usedButton.setAttribute("aria-pressed", String(isUsed));
        usedButton.title = isUsed ? t("markUnused") : t("markUsed");
        usedButton.setAttribute("aria-label", isUsed ? t("markUnusedLabel", { code }) : t("markUsedLabel", { code }));
        if (usedButton.dataset.icon !== String(isUsed)) {
          usedButton.dataset.icon = String(isUsed);
          usedButton.innerHTML = isUsed ? usedIconSvg : unusedIconSvg;
        }
      }

      const expiredButton = card.querySelector(".list-action.expired");
      if (expiredButton) {
        const isExpired = status === "expired";
        expiredButton.setAttribute("aria-pressed", String(isExpired));
        expiredButton.title = isExpired ? t("unmarkExpired") : t("markExpired");
        expiredButton.setAttribute("aria-label", isExpired ? t("unmarkExpiredLabel", { code }) : t("markExpiredLabel", { code }));
      }

      // Stamped chips ship without data-tooltip; keep hover text in sync after cloud metadata loads.
      card.querySelectorAll(".code-date").forEach((chip) => {
        syncDateChip(chip, code, chip.classList.contains("expired-date") ? "expired" : "added");
      });
    }

    function refreshCodeCard(code) {
      const existing = findCodeCard(code);
      if (!codeMatchesCurrentView(code)) {
        existing?.remove();
        updateListEmptyState();
        return;
      }

      const isNew = isNewCode(code);
      const target = isNew ? newCodesEl : codesEl;
      if (existing) {
        if (existing.parentElement !== target) {
          target.appendChild(existing);
        }
        existing.classList.toggle("is-new", isNew);
        patchCodeCard(existing, code);
        updateListEmptyState();
        return;
      }

      target.appendChild(createCodeCard(code, { isNew }));
      updateListEmptyState();
    }

    function refreshAfterCodeStateChange(code, options = {}) {
      refreshCodeCard(code);
      if (options.moveCarousel) {
        moveCarousel(1, { preferUnused: true, carouselOnly: true });
      } else {
        renderCarouselOnly();
      }
      renderProgress();
    }

    function renderCarousel() {
      const carouselCodes = getVisitCarouselCodes();
      const unusedCodes = getUnusedCodes();
      let code = currentIndex === -1 ? null : codes[currentIndex];
      if (unusedCodes.length && (!code || !carouselCodes.includes(code) || getStatus(code) === "expired")) {
        code = unusedCodes[0];
        currentIndex = codes.indexOf(code);
      }
      const status = code ? getStatus(code) : "unused";
      const carouselPosition = code ? carouselCodes.indexOf(code) : -1;
      const showDoneState = !unusedCodes.length && carouselPosition === -1;
      currentCodeEl.textContent = showDoneState ? t("carouselDone") : code;
      desktopCopyHintEl.textContent = unusedCodes.length ? t("desktopCopyHint") : t("carouselNoUnusedHint");
      mobileCopyHintEl.textContent = unusedCodes.length ? t("mobileCopyHint") : t("carouselNoUnusedHint");
      carouselPositionEl.hidden = !showDoneState && carouselPosition === -1;
      if (showDoneState) {
        carouselPositionEl.textContent = t("carouselNoRemaining");
      } else if (carouselPosition !== -1) {
        carouselPositionEl.textContent = t("carouselPosition", { current: carouselPosition + 1, total: carouselCodes.length });
      }
      carouselStatusEl.hidden = showDoneState || status === "unused";
      carouselStatusEl.className = showDoneState ? "badge" : `badge ${status}`;
      carouselStatusEl.textContent = showDoneState ? "" : getStatusLabel(status);
      carouselEl.classList.toggle("is-used", !showDoneState && status === "used");
      carouselEl.classList.toggle("is-done", showDoneState);
      carouselEmptyNoteEl.hidden = true;
      carouselCopyEl.disabled = showDoneState || status !== "unused";
      reportPreviousEl.disabled = !lastCopiedCode;
      reportPreviousEl.title = lastCopiedCode ? t("reportAsExpired", { code: lastCopiedCode }) : t("copyFirst");
      playCarouselAnimation();
    }

    function playCarouselAnimation() {
      if (!pendingCarouselAnimationDirection) return;
      const className = pendingCarouselAnimationDirection > 0
        ? "is-sliding-next"
        : "is-sliding-previous";
      pendingCarouselAnimationDirection = 0;
      carouselCopyEl.classList.remove("is-sliding-next", "is-sliding-previous");
      if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) return;
      void carouselCopyEl.offsetWidth;
      carouselCopyEl.classList.add(className);
      carouselCopyEl.addEventListener("animationend", () => {
        carouselCopyEl.classList.remove(className);
      }, { once: true });
    }

    function createCodeCard(code, options = {}) {
      const status = getStatus(code);
      const card = document.createElement("article");
      card.dataset.code = code;
      card.className = `code-card is-${status}${codes[currentIndex] === code ? " is-current" : ""}${options.isNew ? " is-new" : ""}`;

      const input = document.createElement("input");
      input.className = "code-field";
      input.type = "text";
      input.value = code;
      input.readOnly = true;
      input.setAttribute("aria-label", t("couponCodeLabel", { code }));
      input.addEventListener("focus", () => input.select());

      const copyButton = document.createElement("button");
      copyButton.className = "list-copy";
      copyButton.type = "button";
      copyButton.title = t("copyCode");
      copyButton.disabled = status === "expired";
      copyButton.setAttribute("aria-label", t("copyCodeLabel", { code }));
      copyButton.innerHTML = copyIconSvg;
      copyButton.addEventListener("click", () => copyCode(code));

      const meta = document.createElement("div");
      meta.className = "meta";
      meta.appendChild(createBadge(status));

      const actions = document.createElement("div");
      actions.className = "list-actions";

      const usedButton = document.createElement("button");
      usedButton.className = "list-action used";
      usedButton.type = "button";
      usedButton.title = status === "used" ? t("markUnused") : t("markUsed");
      usedButton.disabled = status === "expired";
      usedButton.setAttribute("aria-pressed", String(status === "used"));
      usedButton.setAttribute("aria-label", status === "used" ? t("markUnusedLabel", { code }) : t("markUsedLabel", { code }));
      usedButton.dataset.icon = String(status === "used");
      usedButton.innerHTML = status === "used" ? usedIconSvg : unusedIconSvg;
      usedButton.addEventListener("click", () => toggleCodeUsed(code));

      const expiredButton = document.createElement("button");
      expiredButton.className = "list-action expired";
      expiredButton.type = "button";
      expiredButton.title = status === "expired" ? t("unmarkExpired") : t("markExpired");
      expiredButton.setAttribute("aria-pressed", String(status === "expired"));
      expiredButton.setAttribute("aria-label", status === "expired" ? t("unmarkExpiredLabel", { code }) : t("markExpiredLabel", { code }));
      expiredButton.innerHTML = expiredIconSvg;
      expiredButton.addEventListener("click", () => openReportDialog(code, status === "expired" ? "unexpired" : "expired"));

      const addedDateChip = createDateChip(code, "added");
      const expiredDateChip = status === "expired" ? createDateChip(code, "expired") : null;
      actions.append(copyButton, usedButton, expiredButton);
      if (addedDateChip) actions.appendChild(addedDateChip);
      if (expiredDateChip) actions.appendChild(expiredDateChip);
      card.append(input, actions, meta);
      card.dataset.hydrated = "1";
      return card;
    }

    function setArchiveOpen(open) {
      if (!archivePanelEl || archivePanelEl.open === open) return;
      archiveOpenSync = true;
      archivePanelEl.open = open;
      // `toggle` can fire after this call returns; keep the sync guard until then.
      queueMicrotask(() => {
        archiveOpenSync = false;
      });
    }

    function renderArchivedExpiredCodes() {
      const query = searchEl.value.trim().toUpperCase();
      const matchingArchived = archivedExpiredCodes.filter((code) => !query || code.includes(query));
      const hasArchive = archivedExpiredCodes.length > 0;
      const filterHidesArchive = activeFilter === "unused" || activeFilter === "used";
      const searchForceOpen = Boolean(query) && matchingArchived.length > 0;

      if (searchForceOpen) {
        archiveSearchExpanded = true;
      } else if (!query && archiveSearchExpanded) {
        // Clearing search should collapse search-driven expansion.
        archiveSearchExpanded = false;
        archivePinnedOpen = false;
      }

      const autoOpen =
        !filterHidesArchive &&
        (activeFilter === "expired" || searchForceOpen);
      const wantOpen = hasArchive && !filterHidesArchive && (autoOpen || archivePinnedOpen);

      if (archivePanelEl) archivePanelEl.hidden = !hasArchive || filterHidesArchive;
      if (archiveCountEl) {
        archiveCountEl.textContent = String(
          query ? matchingArchived.length : archivedExpiredCodes.length
        );
      }

      setArchiveOpen(wantOpen);

      if (!wantOpen) {
        if (archivedCodesEl.childElementCount) archivedCodesEl.replaceChildren();
        return;
      }

      const toShow = matchingArchived;
      if (!toShow.length) {
        archivedCodesEl.replaceChildren();
        return;
      }

      const existingCards = [...archivedCodesEl.querySelectorAll(".code-card[data-code]")];
      const byCode = new Map(existingCards.map((card) => [card.dataset.code, card]));
      const sameOrder =
        existingCards.length === toShow.length &&
        existingCards.every((card, index) => card.dataset.code === toShow[index]);

      if (sameOrder) {
        toShow.forEach((code) => hydrateArchivedCard(byCode.get(code)));
        return;
      }

      const fragment = document.createDocumentFragment();
      toShow.forEach((code) => {
        let card = byCode.get(code);
        if (card) {
          hydrateArchivedCard(card);
          fragment.appendChild(card);
        } else {
          fragment.appendChild(createArchivedExpiredCard(code));
        }
      });
      archivedCodesEl.replaceChildren(fragment);
    }

    function hydrateArchivedCard(card) {
      if (!card || card.dataset.hydrated === "1") return;
      const code = card.dataset.code;
      const input = card.querySelector(".code-field");
      if (input) input.addEventListener("focus", () => input.select());

      const meta = card.querySelector(".meta");
      if (code && meta && !meta.querySelector(".code-date")) {
        const addedDateChip = createDateChip(code, "added");
        const expiredDateChip = createDateChip(code, "expired");
        if (addedDateChip) meta.appendChild(addedDateChip);
        if (expiredDateChip) meta.appendChild(expiredDateChip);
      } else {
        card.querySelectorAll(".code-date").forEach((chip) => {
          if (chip.dataset.bound === "1") return;
          chip.dataset.bound = "1";
          chip.addEventListener("click", (event) => {
            event.stopPropagation();
            const isOpen = chip.classList.contains("is-open");
            document.querySelectorAll(".code-date.is-open").forEach((candidate) => candidate.classList.remove("is-open"));
            chip.classList.toggle("is-open", !isOpen);
          });
        });
      }

      card.dataset.hydrated = "1";
    }

    function createArchivedExpiredCard(code) {
      const card = document.createElement("article");
      card.dataset.code = code;
      card.className = "code-card is-expired is-archived";

      const input = document.createElement("input");
      input.className = "code-field";
      input.type = "text";
      input.value = code;
      input.readOnly = true;
      input.setAttribute("aria-label", t("archivedCodeLabel", { code }));
      input.addEventListener("focus", () => input.select());

      const meta = document.createElement("div");
      meta.className = "meta";
      meta.appendChild(createBadge("expired"));
      const addedDateChip = createDateChip(code, "added");
      const expiredDateChip = createDateChip(code, "expired");
      if (addedDateChip) meta.appendChild(addedDateChip);
      if (expiredDateChip) meta.appendChild(expiredDateChip);

      card.append(input, meta);
      card.dataset.hydrated = "1";
      return card;
    }

    function createBadge(status) {
      const badge = document.createElement("span");
      badge.className = `badge ${status}`;
      badge.textContent = getStatusLabel(status);
      return badge;
    }

    function getStatusLabel(status) {
      const labels = {
        unused: t("statusUnused"),
        used: t("statusUsed"),
        expired: t("statusExpired")
      };
      return labels[status] || t("statusUnused");
    }

    async function copyCode(code, options = {}) {
      // Start clipboard immediately (keeps user-gesture), then paint UI without waiting on it.
      const clipboardPromise = writeToClipboard(code);
      try {
        setCodeState(code, { used: true, copiedAt: new Date().toISOString() }, { skipRender: true });
        showToast(t(options.advance ? "copiedAdvance" : "copiedUsed", { code }));
        const shouldAdvance = Boolean(options.advance) || codes[currentIndex] === code;
        if (options.advance) lastCopiedCode = code;
        await yieldForPaint();
        refreshAfterCodeStateChange(code, { moveCarousel: shouldAdvance });
        await clipboardPromise;
      } catch {
        clearCodeState(code);
        refreshAfterCodeStateChange(code);
        showToast(t("copyFailed"));
      }
    }

    async function writeToClipboard(text) {
      if (navigator.clipboard?.writeText) {
        await navigator.clipboard.writeText(text);
        return;
      }

      const textArea = document.createElement("textarea");
      textArea.value = text;
      textArea.setAttribute("readonly", "");
      textArea.style.position = "fixed";
      textArea.style.opacity = "0";
      document.body.appendChild(textArea);
      textArea.select();
      const copied = document.execCommand("copy");
      textArea.remove();
      if (!copied) throw new Error("Clipboard unavailable");
    }

    async function copyCarouselCode() {
      const code = codes[currentIndex];
      if (!code || getStatus(code) === "expired") return;
      await copyCode(code, { advance: true });
    }

    async function toggleCodeUsed(code) {
      if (getStatus(code) === "used") {
        clearCodeState(code);
        currentIndex = Math.max(0, codes.indexOf(code));
        showToast(t("addedBack", { code }));
        await yieldForPaint();
        refreshAfterCodeStateChange(code);
        return;
      }

      setCodeState(code, { used: true, copiedAt: new Date().toISOString() }, { skipRender: true });
      const moved = codes[currentIndex] === code;
      showToast(t("markedUsed", { code }));
      await yieldForPaint();
      refreshAfterCodeStateChange(code, { moveCarousel: moved });
    }

    async function copyAllCodes() {
      const activeCodes = codes.filter((code) => getStatus(code) !== "expired");
      if (!activeCodes.length) {
        showToast(t("noActiveCodes"));
        return;
      }

      if (!confirm(t("copyAllConfirm", { count: activeCodes.length }))) return;
      try {
        await writeToClipboard(activeCodes.join("\n"));
        showToast(t("activeCopied"));
      } catch {
        showToast(t("copyAllFailed"));
      }
    }

    async function moveCarousel(direction, options = {}) {
      const finishMove = async () => {
        await yieldForPaint();
        if (options.carouselOnly) renderCarouselOnly();
        else render();
      };
      const carouselCodes = getVisitCarouselCodes();
      if (!carouselCodes.length) {
        currentIndex = -1;
        await finishMove();
        return;
      }
      const previousIndex = currentIndex;
      const currentCode = currentIndex === -1 ? null : codes[currentIndex];
      const currentQueueIndex = currentCode && carouselCodes.includes(currentCode)
        ? carouselCodes.indexOf(currentCode)
        : direction < 0 ? carouselCodes.length : -1;

      for (let offset = 1; offset <= carouselCodes.length; offset += 1) {
        const candidateQueueIndex = (currentQueueIndex + direction * offset + carouselCodes.length) % carouselCodes.length;
        const candidateCode = carouselCodes[candidateQueueIndex];
        const candidateStatus = getStatus(candidateCode);
        const matchesMode = options.preferUnused
          ? candidateStatus === "unused"
          : candidateStatus !== "expired";

        if (matchesMode) {
          currentIndex = codes.indexOf(candidateCode);
          pendingCarouselAnimationDirection = currentIndex === previousIndex ? 0 : direction;
          await finishMove();
          return;
        }
      }

      currentIndex = options.preferUnused ? -1 : codes.indexOf(carouselCodes[0]);
      pendingCarouselAnimationDirection = currentIndex === previousIndex ? 0 : direction;
      await finishMove();
    }

    function reportLastCopiedCode() {
      if (!lastCopiedCode) return;
      openReportDialog(lastCopiedCode, "expired");
    }

    function selectedExpiredError() {
      return expiredErrorInputs.find((input) => input.checked)?.value || "";
    }

    function isExpiredReportError(value) {
      return value === "expired" || value === "attempts-used-up";
    }

    function resetExpiredErrorSelection() {
      expiredErrorInputs.forEach((input) => {
        input.checked = false;
      });
      expiredErrorRow.hidden = true;
    }

    function hideDialogCaptcha() {
      const widget = dialog.querySelector(".captcha-widget");
      destroyCaptchaWidget(widget);
      if (widget) widget.hidden = true;
    }

    function updateExpiredDialogConfirm() {
      if (pendingReport?.report !== "expired") return;
      const error = selectedExpiredError();
      dialogConfirm.disabled = !error;
      dialogConfirm.textContent = error && !isExpiredReportError(error) ? t("confirm") : t("reportExpired");
      if (isExpiredReportError(error)) {
        scheduleCaptchaWidget(dialog);
      } else {
        hideDialogCaptcha();
      }
    }

    function showNonExpiredReportHelp(code, error) {
      pendingReport = { code, report: "expired-info" };
      resetExpiredErrorSelection();
      hideDialogCaptcha();
      dialogTitle.textContent = error === "already-used"
        ? t("expiredAlreadyUsedTitle")
        : t("expiredTooFrequentTitle");
      dialogMessage.textContent = error === "already-used"
        ? t("expiredAlreadyUsedHelp")
        : t("expiredTooFrequentHelp");
      dialogCodeRow.hidden = true;
      dialogCode.textContent = "";
      dialogCancel.hidden = true;
      dialogConfirm.textContent = t("confirm");
      dialogConfirm.disabled = false;
      dialogConfirm.classList.toggle("primary", true);
      dialog.returnValue = "";
      dialog.showModal();
    }

    async function openReportDialog(code, report) {
      if (report === "expired") {
        const data = await fetchCodesPayload();
        if (data) {
          const normalized = String(code || "").trim().toUpperCase();
          const activeSet = new Set(normalizeCodeList(data.active).map(codeValue));
          const expiredSet = new Set(normalizeCodeList(data.expired).map(codeValue));
          const listedStatus = expiredSet.has(normalized)
            ? "expired"
            : activeSet.has(normalized)
              ? "active"
              : null;

          if (listedStatus !== "active") {
            showToast(
              listedStatus === "expired"
                ? t("alreadyExpiredReportStale", { code })
                : t("codeNotListedReportStale", { code })
            );
            promptCodesOutOfDate(data);
            return;
          }

          if (codesFingerprint(data) !== appliedCodesFingerprint) {
            showCodesUpdateBanner(data);
          }
        }
      }

      pendingReport = { code, report };
      const isExpired = report === "expired";
      const verifiedAt = codeVerifiedAt(code);
      dialogTitle.textContent = isExpired ? t("reportExpiredTitle") : t("unmarkExpiredTitle");
      const baseMessage = isExpired ? t("reportExpiredMessage") : t("unmarkExpiredMessage");
      dialogMessage.textContent = verifiedAt
        ? `${baseMessage}\n\n${t("lastVerifiedLine", { verifiedAt: formatLocalStamp(verifiedAt) })}`
        : baseMessage;
      dialogCodeRow.hidden = false;
      dialogCode.textContent = code;
      dialogConfirm.textContent = isExpired ? t("reportExpired") : t("unmarkExpired");
      dialogCancel.hidden = false;
      resetExpiredErrorSelection();
      expiredErrorRow.hidden = !isExpired;
      dialogConfirm.disabled = isExpired;
      dialogConfirm.classList.toggle("primary", true);
      hideDialogCaptcha();
      dialog.returnValue = "";
      dialog.showModal();
      if (!isExpired) scheduleCaptchaWidget(dialog);
    }

    async function copyPendingReportCode() {
      if (!pendingReport?.code) return;
      try {
        await writeToClipboard(pendingReport.code);
        showToast(t("copied", { code: pendingReport.code }));
      } catch {
        showToast(t("copyFailed"));
      }
    }

    function yieldForPaint() {
      if (typeof scheduler !== "undefined" && typeof scheduler.yield === "function") {
        return scheduler.yield();
      }
      return new Promise((resolve) => {
        requestAnimationFrame(() => setTimeout(resolve, 0));
      });
    }

    async function handleDialogClose() {
      if (dialog.returnValue !== "confirm" || !pendingReport) {
        pendingReport = null;
        resetExpiredErrorSelection();
        dialogCodeRow.hidden = false;
        dialogCancel.hidden = false;
        dialogConfirm.disabled = false;
        hideDialogCaptcha();
        return;
      }

      const { code, report } = pendingReport;
      if (report === "expired-info") {
        pendingReport = null;
        resetExpiredErrorSelection();
        dialogCodeRow.hidden = false;
        dialogCancel.hidden = false;
        dialogConfirm.disabled = false;
        hideDialogCaptcha();
        return;
      }

      if (report === "expired") {
        const error = selectedExpiredError();
        if (!error) {
          dialogConfirm.disabled = true;
          dialog.returnValue = "";
          dialog.showModal();
          hideDialogCaptcha();
          return;
        }
        if (!isExpiredReportError(error)) {
          await yieldForPaint();
          showNonExpiredReportHelp(code, error);
          return;
        }
      }

      const captchaToken = getCaptchaToken(dialog);
      if (captchaToken === null) {
        dialog.returnValue = "";
        dialog.showModal();
        scheduleCaptchaWidget(dialog);
        return;
      }

      pendingReport = null;
      resetExpiredErrorSelection();
      dialogCodeRow.hidden = false;
      dialogCancel.hidden = false;
      dialogConfirm.disabled = false;

      // Let the dialog close paint before list/carousel work (INP).
      await yieldForPaint();

      if (report === "expired") {
        const shouldAdvance = codes[currentIndex] === code;
        setCodeState(code, { report, used: true, reportedAt: new Date().toISOString() }, { skipRender: true });
        if (lastCopiedCode === code) lastCopiedCode = null;
        refreshAfterCodeStateChange(code, { moveCarousel: shouldAdvance });
        await reportCodeReport(code, "expired", { captchaToken, captchaForm: dialog });
        return;
      }

      if (report === "unexpired") {
        clearCodeState(code);
        currentIndex = Math.max(0, codes.indexOf(code));
        refreshAfterCodeStateChange(code);
        await reportCodeReport(code, "unexpired", { captchaToken, captchaForm: dialog });
      }
    }

    async function reportCodeReport(code, report, options = {}) {
      try {
        const response = await fetch(reportEndpoint, {
          method: "POST",
          headers: { "Content-Type": "application/json; charset=utf-8" },
          body: JSON.stringify({
            code,
            report,
            turnstileToken: options.captchaToken || "",
            browserId: getBrowserSubmissionId(),
            browserIdCreatedAt: getBrowserSubmissionIdCreatedAt(),
            page: location.href,
            reportedAt: new Date().toISOString()
          })
        });

        if (!response.ok) {
          const errorPayload = await response.json().catch(() => ({}));
          if (response.status === 409 && (report === "new-code" || report === "new-expired-code")) {
            const status = errorPayload.status || "submitted";
            if (status === "submitted") rememberSubmittedCode(code);
            showNewCodeDuplicateMessage(status);
            if (errorPayload.pageMayBeOutdated || status === "active" || status === "expired") {
              showToast(
                status === "expired"
                  ? t("duplicateExpiredStale")
                  : t("duplicateActiveStale")
              );
              promptCodesOutOfDate();
            }
            newCodeSubmit.disabled = true;
            newCodeDialog.showModal();
            newCodeInput.focus();
            if (options.captchaForm) resetCaptcha(options.captchaForm);
            return false;
          }
          if (response.status === 403) {
            showToast(getCaptchaFailureMessage(errorPayload));
            if (options.captchaForm) resetCaptcha(options.captchaForm);
            return false;
          }
          if (response.status === 429) {
            showToast(t("reportRateLimited"));
            if (options.captchaForm) resetCaptcha(options.captchaForm);
            return false;
          }
          if (response.status === 409 && report === "expired") {
            showToast(
              errorPayload.status === "expired"
                ? t("alreadyExpiredReportStale", { code })
                : t("codeNotListedReportStale", { code })
            );
            if (errorPayload.pageMayBeOutdated || errorPayload.status === "expired" || errorPayload.status === "unknown") {
              promptCodesOutOfDate();
            }
            if (options.captchaForm) resetCaptcha(options.captchaForm);
            return false;
          }
          throw new Error("Report failed");
        }
        showToast(getReportSuccessMessage(code, report));
        if (options.captchaForm) resetCaptcha(options.captchaForm);
        return true;
      } catch {
        showToast(getReportFallbackMessage(code, report));
        if (options.captchaForm) resetCaptcha(options.captchaForm);
        return false;
      }
    }

    function getReportSuccessMessage(code, report) {
      if (report === "expired") return t("reportedExpired", { code });
      if (report === "unexpired") return t("restored", { code });
      if (report === "new-expired-code") return t("submittedExpiredArchive", { code });
      return t("submittedReview", { code });
    }

    function getReportFallbackMessage(code, report) {
      if (report === "expired") return t("expiredPreview", { code });
      if (report === "unexpired") return t("restoredPreview", { code });
      return t("submitPreview", { code });
    }

    function getCaptchaFailureMessage(payload = {}) {
      return payload.captchaError
        ? t("captchaFailedDetail", { reason: payload.captchaError })
        : t("captchaFailed");
    }

    function openNewCodeDialog() {
      newCodeInput.value = "";
      newCodeExpiredCheck.checked = false;
      newCodeCheckedCheck.checked = false;
      newCodeSubmit.disabled = true;
      clearNewCodeMessage();
      destroyCaptchaWidget(newCodeForm.querySelector(".captcha-widget"));
      newCodeDialog.showModal();
      scheduleCaptchaWidget(newCodeForm);
      newCodeInput.focus();
      // On-demand freshness check only (no background polling) before submit validation.
      loadCloudCodes().finally(refreshNewCodeValidation);
    }

    function clearNewCodeMessage() {
      newCodeMessage.textContent = "";
      newCodeMessage.classList.remove("is-active", "is-expired");
      newCodeInput.classList.remove("is-duplicate-active", "is-duplicate-expired");
    }

    function showNewCodeDuplicateMessage(status) {
      const isExpired = status === "expired";
      newCodeMessage.textContent = isExpired
        ? t("duplicateExpired")
        : status === "submitted"
          ? t("duplicateSubmitted")
          : t("duplicateActive");
      newCodeMessage.classList.toggle("is-expired", isExpired);
      newCodeMessage.classList.toggle("is-active", !isExpired);
      newCodeInput.classList.toggle("is-duplicate-expired", isExpired);
      newCodeInput.classList.toggle("is-duplicate-active", !isExpired);
    }

    function getNewCodeConflictStatus(code = newCodeInput.value.trim().toUpperCase()) {
      if (!code) return null;
      return getListedCodeStatus(code) || (hasSubmittedCode(code) ? "submitted" : null);
    }

    function refreshNewCodeValidation() {
      const code = newCodeInput.value.trim().toUpperCase();
      const conflict = getNewCodeConflictStatus(code);
      if (conflict) {
        showNewCodeDuplicateMessage(conflict);
      } else {
        clearNewCodeMessage();
      }
      newCodeSubmit.disabled = !newCodeCheckedCheck.checked || !code || Boolean(conflict);
    }

    async function submitNewCode(event) {
      event.preventDefault();
      const code = newCodeInput.value.trim().toUpperCase();
      if (!code) return;
      if (code.length > 16) {
        showToast(t("tooLong"));
        return;
      }
      if (!newCodeCheckedCheck.checked) {
        showToast(t("confirmChecked"));
        return;
      }

      // Re-fetch before the final check so a late cloud list cannot race past validation.
      await loadCloudCodes();
      const conflict = getNewCodeConflictStatus(code);
      if (conflict) {
        showNewCodeDuplicateMessage(conflict);
        refreshNewCodeValidation();
        newCodeInput.focus();
        return;
      }

      const captchaToken = getCaptchaToken(newCodeForm);
      if (captchaToken === null) return;

      newCodeDialog.close();
      const submitted = await reportCodeReport(
        code,
        newCodeExpiredCheck.checked ? "new-expired-code" : "new-code",
        { captchaToken, captchaForm: newCodeForm }
      );
      if (submitted) rememberSubmittedCode(code);
    }

    function openContactDialog() {
      contactMessage.value = "";
      contactSubmit.disabled = true;
      destroyCaptchaWidget(contactForm.querySelector(".captcha-widget"));
      contactDialog.showModal();
      scheduleCaptchaWidget(contactForm);
    }

    function openCompareDialog() {
      compareCodesInput.value = "";
      renderCompareResults();
      compareDialog.showModal();
      compareCodesInput.focus();
    }

    function parsePastedCodes(text) {
      const seen = new Set();
      const codesList = [];
      String(text || "")
        .toUpperCase()
        .split(/[\s,]+/)
        .forEach((token) => {
          const code = token.trim();
          if (!code || seen.has(code)) return;
          seen.add(code);
          codesList.push(code);
        });
      return codesList;
    }

    function getCompareBuckets(pastedCodes) {
      const siteActive = new Set(codes);
      const siteExpired = new Set(archivedExpiredCodes);
      const pastedSet = new Set(pastedCodes);
      const matched = [];
      const expired = [];
      const unknown = [];
      const newer = [];

      pastedCodes.forEach((code) => {
        if (siteActive.has(code)) matched.push(code);
        else if (siteExpired.has(code)) expired.push(code);
        else unknown.push(code);
      });

      codes.forEach((code) => {
        if (!pastedSet.has(code)) newer.push(code);
      });

      return { new: newer, matched, expired, unknown };
    }

    function fillCompareGroup(group, items, chipClass) {
      group.codes.innerHTML = "";
      group.count.textContent = `(${items.length})`;
      group.section.hidden = !items.length;
      items.forEach((code) => {
        const chip = document.createElement("span");
        chip.className = `compare-chip ${chipClass}`;
        chip.textContent = code;
        group.codes.appendChild(chip);
      });
    }

    function renderCompareResults() {
      const pastedCodes = parsePastedCodes(compareCodesInput.value);
      if (!pastedCodes.length) {
        lastCompareBuckets = { new: [], matched: [], expired: [], unknown: [] };
        compareEmptyEl.hidden = false;
        compareResultsEl.hidden = true;
        compareMarkUsedEl.disabled = true;
        Object.values(compareGroups).forEach((group) => {
          group.section.hidden = true;
          group.codes.innerHTML = "";
          group.count.textContent = "";
          if (group.copy) group.copy.disabled = true;
        });
        return;
      }

      const buckets = getCompareBuckets(pastedCodes);
      lastCompareBuckets = buckets;
      fillCompareGroup(compareGroups.new, buckets.new, "is-new");
      fillCompareGroup(compareGroups.matched, buckets.matched, "is-matched");
      fillCompareGroup(compareGroups.expired, buckets.expired, "is-expired");
      fillCompareGroup(compareGroups.unknown, buckets.unknown, "is-unknown");

      compareEmptyEl.hidden = true;
      compareResultsEl.hidden = false;
      const markableCount = buckets.matched.length + buckets.expired.length;
      compareMarkUsedEl.disabled = markableCount === 0;
      Object.entries(compareCopyButtons).forEach(([kind, button]) => {
        button.disabled = !(buckets[kind] && buckets[kind].length);
      });
    }

    async function copyCompareBucket(kind) {
      const items = lastCompareBuckets[kind] || [];
      if (!items.length) {
        showToast(t("compareNothingToCopy"));
        return;
      }

      try {
        await writeToClipboard(items.join("\n"));
        const toastKey = ({
          new: "compareCopiedNew",
          matched: "compareCopiedMatched",
          expired: "compareCopiedExpired",
          unknown: "compareCopiedMissing"
        })[kind] || "compareCopiedMissing";
        showToast(t(toastKey, { count: items.length }));
      } catch {
        showToast(t("copyFailed"));
      }
    }

    function markComparedCodesUsed() {
      const pastedCodes = parsePastedCodes(compareCodesInput.value);
      const siteCodes = new Set([...codes, ...archivedExpiredCodes]);
      const now = new Date().toISOString();
      let marked = 0;

      pastedCodes.forEach((code) => {
        if (!siteCodes.has(code)) return;
        const current = getCodeState(code);
        if (current.used) return;
        state[code] = {
          ...current,
          used: true,
          copiedAt: current.copiedAt || now,
          updatedAt: now
        };
        marked += 1;
      });

      if (!marked) {
        showToast(t("compareNoneToMark"));
        return;
      }

      saveState();
      render();
      renderCompareResults();
      showToast(t("compareMarkedUsed", { count: marked }));
    }

    async function submitContactMessage(event) {
      event.preventDefault();
      const message = contactMessage.value.trim();
      if (!message) {
        showToast(t("messageRequired"));
        return;
      }

      contactSubmit.disabled = true;
      const captchaToken = getCaptchaToken(contactForm);
      if (captchaToken === null) {
        contactSubmit.disabled = false;
        return;
      }

      try {
        const response = await fetch(reportEndpoint, {
          method: "POST",
          headers: { "Content-Type": "application/json; charset=utf-8" },
          body: JSON.stringify({
            report: "contact",
            message,
            turnstileToken: captchaToken,
            browserId: getBrowserSubmissionId(),
            browserIdCreatedAt: getBrowserSubmissionIdCreatedAt(),
            page: location.href,
            reportedAt: new Date().toISOString()
          })
        });

        if (!response.ok) {
          const errorPayload = await response.json().catch(() => ({}));
          if (response.status === 403) {
            showToast(getCaptchaFailureMessage(errorPayload));
            resetCaptcha(contactForm);
            return;
          }
          throw new Error("Contact failed");
        }
        contactDialog.close();
        contactMessage.value = "";
        resetCaptcha(contactForm);
        showToast(t("messageSent"));
      } catch {
        showToast(t("messageFailed"));
        resetCaptcha(contactForm);
      } finally {
        contactSubmit.disabled = !contactMessage.value.trim();
      }
    }

    function resetState() {
      if (!confirm(t("resetConfirm"))) return;
      Object.keys(state).forEach((key) => delete state[key]);
      currentIndex = 0;
      lastCopiedCode = null;
      saveState();
      render();
      showToast(t("resetDone"));
    }

    function renderProgress() {
      const totals = codes.reduce((summary, code) => {
        const status = getStatus(code);
        summary.total += 1;
        if (status !== "unused") summary.used += 1;
        return summary;
      }, { total: 0, used: 0 });

      progressLabelEl.textContent = t("progressUsed", { used: totals.used, total: totals.total });
      remainingCountEl.textContent = t("remaining", { count: totals.total - totals.used });
      document.querySelector("#progress-fill").style.width = totals.total
        ? `${Math.round((totals.used / totals.total) * 100)}%`
        : "0%";
    }

    function showToast(message) {
      const toast = document.createElement("div");
      toast.className = "toast-message";
      toast.textContent = message;
      toastEl.appendChild(toast);
      setTimeout(() => {
        toast.remove();
      }, 3600);
    }
  </script>
  <!-- Cloudflare Web Analytics (deferred until after load) -->
  <script>
    window.addEventListener("load", function () {
      var loadBeacon = function () {
        var s = document.createElement("script");
        s.src = "https://static.cloudflareinsights.com/beacon.min.js";
        s.setAttribute("data-cf-beacon", '{"token": "0143baf38c2b4a66b9f5cffdfe0c5ed1"}');
        s.defer = true;
        document.body.appendChild(s);
      };
      if ("requestIdleCallback" in window) {
        requestIdleCallback(loadBeacon, { timeout: 3000 });
      } else {
        setTimeout(loadBeacon, 1);
      }
    });
  </script>
  <!-- End Cloudflare Web Analytics -->
</body>
</html>
